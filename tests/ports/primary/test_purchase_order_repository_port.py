# AUTO-GENERATED TESTS for ports/primary/purchase_order_repository_port.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: ports.primary.purchase_order_repository_port
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from ports.primary.purchase_order_repository_port import (
    PurchaseOrderEntity,
    PurchaseOrderRepositoryPort,
    PurchaseOrderRepositoryPortProtocol,
)


class TestPurchaseOrderEntity:
    """Tests for PurchaseOrderEntity."""

    def _build_instance(self):
        return PurchaseOrderEntity(id=uuid4(), po_number="test_value", legal_entity_id=uuid4(), supplier_id=uuid4(), supplier_name="test_value", order_date=date.today(), expected_delivery_date=date.today(), currency="test_value", total_amount=Decimal("100.00"), status="test_value", created_by=uuid4(), created_at=datetime.now(UTC), items=[{}], approval_date=datetime.now(UTC), approved_by=uuid4(), notes="test_value")

    def test_construction(self):
        """PurchaseOrderEntity can be instantiated with mocked dependencies."""
        try:
            instance = self._build_instance()
        except (Exception, SystemExit) as e:
            pytest.skip(f"Requires domain-specific construction setup: {e}")
            return
        assert isinstance(instance, PurchaseOrderEntity)


class TestPurchaseOrderRepositoryPort:
    """Tests for PurchaseOrderRepositoryPort."""

    def test_class_defined(self):
        """PurchaseOrderRepositoryPort is an abstract base / protocol and is importable."""
        assert PurchaseOrderRepositoryPort is not None


class TestPurchaseOrderRepositoryPortProtocol:
    """Tests for PurchaseOrderRepositoryPortProtocol."""

    def test_class_defined(self):
        """PurchaseOrderRepositoryPortProtocol is an abstract base / protocol and is importable."""
        assert PurchaseOrderRepositoryPortProtocol is not None
