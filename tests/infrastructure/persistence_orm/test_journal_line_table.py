# AUTO-GENERATED TESTS for infrastructure/persistence_orm/journal_line_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.journal_line_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.journal_line_table import JournalLineTable


class TestJournalLineTable:
    """Tests for the JournalLineTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(JournalLineTable, '__tablename__')
        assert isinstance(JournalLineTable.__tablename__, str)
        assert len(JournalLineTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            journal_id=uuid4(),
            account_code="test_value",
            line_number=1,
            description="test_value",
            debit_amount=Decimal("100.00"),
            credit_amount=Decimal("0"),
            currency="test_value",
            cost_center="test_value",
            department="test_value",
            account_name="test_value",
            audit_metadata={},
            journal=MagicMock(),
            account=MagicMock(),
        )
        try:
            instance = JournalLineTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, JournalLineTable)
