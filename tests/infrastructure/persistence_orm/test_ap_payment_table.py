# AUTO-GENERATED TESTS for infrastructure/persistence_orm/ap_payment_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.ap_payment_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.ap_payment_table import APPaymentTable


class TestAPPaymentTable:
    """Tests for the APPaymentTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(APPaymentTable, '__tablename__')
        assert isinstance(APPaymentTable.__tablename__, str)
        assert len(APPaymentTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': MagicMock(),
            'payment_number': "test_value",
            'payment_date': date.today(),
            'supplier_id': MagicMock(),
            'invoice_id': MagicMock(),
            'total_amount': Decimal("100.00"),
            'currency': "test_value",
            'payment_method': "test_value",
            'status': "test_value",
            'reference_number': "test_value",
            'bank_reference': "test_value",
            'payment_run_id': MagicMock(),
            'approved_by': MagicMock(),
            'approved_at': datetime.now(UTC),
            'created_by': MagicMock(),
            'supplier': MagicMock(),
            'invoice': MagicMock(),
            'bupots': [MagicMock()],
        }
        try:
            instance = APPaymentTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, APPaymentTable)
