# AUTO-GENERATED TESTS for infrastructure/persistence_orm/derivative_instrument_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.derivative_instrument_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.derivative_instrument_table import DerivativeInstrumentTable


class TestDerivativeInstrumentTable:
    """Tests for the DerivativeInstrumentTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(DerivativeInstrumentTable, '__tablename__')
        assert isinstance(DerivativeInstrumentTable.__tablename__, str)
        assert len(DerivativeInstrumentTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            legal_entity_id=uuid4(),
            instrument_code="test_value",
            instrument_type="test_value",
            counterparty_id=uuid4(),
            underlying_asset="test_value",
            notional_amount=Decimal("100.00"),
            currency_code="test_value",
            settlement_date=date.today(),
            maturity_date=date.today(),
            strike_price=Decimal("100.00"),
            premium_paid=Decimal("100.00"),
            fair_value_at_reporting=Decimal("100.00"),
            valuation_method="test_value",
            is_designated_hedge=True,
            hedging_relationship_id=uuid4(),
            status="test_value",
            version=1,
            created_at=datetime.now(UTC),
            created_by=uuid4(),
        )
        try:
            instance = DerivativeInstrumentTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, DerivativeInstrumentTable)
