# AUTO-GENERATED TESTS for infrastructure/persistence_orm/bank_transaction_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.bank_transaction_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.bank_transaction_table import BankTransactionTable


class TestBankTransactionTable:
    """Tests for the BankTransactionTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(BankTransactionTable, '__tablename__')
        assert isinstance(BankTransactionTable.__tablename__, str)
        assert len(BankTransactionTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            transaction_number="test_value",
            bank_account_id=uuid4(),
            transaction_date=date.today(),
            transaction_type="test_value",
            amount=Decimal("100.00"),
            currency_code="test_value",
            description="test_value",
            reference_number="test_value",
            counterparty_account="test_value",
            counterparty_name="test_value",
            journal_id=MagicMock(),
            status="test_value",
            is_reconciled=True,
            reconciliation_id=MagicMock(),
            created_by=MagicMock(),
            bank_account=MagicMock(),
        )
        try:
            instance = BankTransactionTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, BankTransactionTable)
