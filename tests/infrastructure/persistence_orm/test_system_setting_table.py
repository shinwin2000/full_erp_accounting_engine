# AUTO-GENERATED TESTS for infrastructure/persistence_orm/system_setting_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.system_setting_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.system_setting_table import SystemSettingTable


class TestSystemSettingTable:
    """Tests for the SystemSettingTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(SystemSettingTable, '__tablename__')
        assert isinstance(SystemSettingTable.__tablename__, str)
        assert len(SystemSettingTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            key="test_value",
            data_type="test_value",
            value="test_value",
            description="test_value",
            category="test_value",
            scope="test_value",
            validation_regex="test_value",
            min_value="test_value",
            max_value="test_value",
            allowed_values=["test_value"],
            default_value="test_value",
            is_readonly=True,
            is_encrypted=True,
            is_active=True,
            created_by=MagicMock(),
            updated_by=MagicMock(),
        )
        try:
            instance = SystemSettingTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, SystemSettingTable)
