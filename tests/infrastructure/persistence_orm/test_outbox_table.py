# AUTO-GENERATED TESTS for infrastructure/persistence_orm/outbox_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.outbox_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.outbox_table import OutboxStatus, OutboxTable


class TestOutboxStatus:
    """Tests for OutboxStatus."""

    def _build_instance(self):
        return OutboxStatus()

    def test_construction(self):
        """OutboxStatus can be instantiated with mocked dependencies."""
        try:
            instance = self._build_instance()
        except (Exception, SystemExit) as e:
            pytest.skip(f"Requires domain-specific construction setup: {e}")
            return
        assert isinstance(instance, OutboxStatus)


class TestOutboxTable:
    """Tests for the OutboxTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(OutboxTable, '__tablename__')
        assert isinstance(OutboxTable.__tablename__, str)
        assert len(OutboxTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            event_type="test_value",
            aggregate_id=MagicMock(),
            aggregate_type="test_value",
            payload="test_value",
            status="test_value",
            retry_count=1,
            last_error="test_value",
            next_retry_at=datetime.now(UTC),
            sent_at=datetime.now(UTC),
            legal_entity_id=MagicMock(),
            event_id=MagicMock(),
            idempotency_key="test_value",
            processed_at=datetime.now(UTC),
            version=1,
            priority=1,
            scheduled_at=datetime.now(UTC),
            correlation_id="test_value",
        )
        try:
            instance = OutboxTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, OutboxTable)
