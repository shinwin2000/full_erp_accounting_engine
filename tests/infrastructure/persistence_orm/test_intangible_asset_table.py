# AUTO-GENERATED TESTS for infrastructure/persistence_orm/intangible_asset_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.intangible_asset_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.intangible_asset_table import (
    AmortizationMethod,
    IntangibleAssetTable,
    IntangibleAssetType,
)


class TestIntangibleAssetType:
    """Tests for the IntangibleAssetType enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(IntangibleAssetType, 'PATENT')
        assert hasattr(IntangibleAssetType, 'TRADEMARK')
        assert hasattr(IntangibleAssetType, 'COPYRIGHT')
        assert hasattr(IntangibleAssetType, 'SOFTWARE')
        assert hasattr(IntangibleAssetType, 'LICENSE')
        assert hasattr(IntangibleAssetType, 'GOODWILL')
        assert hasattr(IntangibleAssetType, 'CUSTOMER_RELATIONSHIP')
        assert hasattr(IntangibleAssetType, 'OTHER')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(IntangibleAssetType.PATENT, IntangibleAssetType)


class TestAmortizationMethod:
    """Tests for the AmortizationMethod enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(AmortizationMethod, 'STRAIGHT_LINE')
        assert hasattr(AmortizationMethod, 'DECLINING_BALANCE')
        assert hasattr(AmortizationMethod, 'UNITS_OF_PRODUCTION')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(AmortizationMethod.STRAIGHT_LINE, AmortizationMethod)


class TestIntangibleAssetTable:
    """Tests for the IntangibleAssetTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(IntangibleAssetTable, '__tablename__')
        assert isinstance(IntangibleAssetTable.__tablename__, str)
        assert len(IntangibleAssetTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': MagicMock(),
            'asset_code': "test_value",
            'asset_name': "test_value",
            'asset_type': IntangibleAssetType.PATENT,
            'description': "test_value",
            'legal_entity_id': MagicMock(),
            'acquisition_date': date.today(),
            'acquisition_cost': Decimal("100.00"),
            'residual_value': Decimal("100.00"),
            'useful_life_years': 1,
            'amortization_method': AmortizationMethod.STRAIGHT_LINE,
            'accumulated_amortization': Decimal("100.00"),
            'last_amortization_date': date.today(),
            'carrying_amount': Decimal("100.00"),
            'impairment_loss': Decimal("100.00"),
            'impairment_date': date.today(),
            'revaluation_surplus': Decimal("100.00"),
            'is_active': True,
            'disposed_date': date.today(),
            'disposal_amount': Decimal("100.00"),
            'supporting_document_url': "test_value",
            'created_by': MagicMock(),
            'amortization_schedules': [MagicMock()],
            'revaluations': [MagicMock()],
        }
        try:
            instance = IntangibleAssetTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, IntangibleAssetTable)
