# AUTO-GENERATED TESTS for infrastructure/persistence_orm/coretax_bupot_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.coretax_bupot_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.coretax_bupot_table import (
    BupotStatus,
    BupotType,
    CoretaxBupotTable,
)


class TestBupotType:
    """Tests for the BupotType enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(BupotType, 'PPH21')
        assert hasattr(BupotType, 'PPH23')
        assert hasattr(BupotType, 'PPH26')
        assert hasattr(BupotType, 'PPH4_AYAT2')
        assert hasattr(BupotType, 'PPH_FINAL_LAIN')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(BupotType.PPH21, BupotType)


class TestBupotStatus:
    """Tests for the BupotStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(BupotStatus, 'DRAFT')
        assert hasattr(BupotStatus, 'SUBMITTED')
        assert hasattr(BupotStatus, 'APPROVED')
        assert hasattr(BupotStatus, 'REJECTED')
        assert hasattr(BupotStatus, 'VOID')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(BupotStatus.DRAFT, BupotStatus)


class TestCoretaxBupotTable:
    """Tests for the CoretaxBupotTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(CoretaxBupotTable, '__tablename__')
        assert isinstance(CoretaxBupotTable.__tablename__, str)
        assert len(CoretaxBupotTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            bupot_number="test_value",
            bupot_type=BupotType.PPH21,
            status=BupotStatus.DRAFT,
            taxpayer_npwp="test_value",
            taxpayer_name="test_value",
            taxpayer_address="test_value",
            transaction_date=date.today(),
            tax_period_month=1,
            tax_period_year=1,
            gross_amount=Decimal("100.00"),
            tax_rate=Decimal("100.00"),
            tax_amount=Decimal("100.00"),
            withholding_amount=Decimal("100.00"),
            tax_object_description="test_value",
            reference_document_number="test_value",
            reference_document_date=date.today(),
            coretax_submission_id="test_value",
            coretax_status_code="test_value",
            coretax_status_description="test_value",
            coretax_response_raw={},
            coretax_submitted_at=datetime.now(UTC),
            coretax_approved_at=datetime.now(UTC),
            void_reason="test_value",
            void_by=MagicMock(),
            void_at=datetime.now(UTC),
            invoice_id=MagicMock(),
            purchase_invoice_id=MagicMock(),
            payment_id=MagicMock(),
            invoice=MagicMock(),
            purchase_invoice=MagicMock(),
            payment=MagicMock(),
        )
        try:
            instance = CoretaxBupotTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, CoretaxBupotTable)
