# AUTO-GENERATED TESTS for infrastructure/persistence_orm/project_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.project_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.project_table import ProjectTable


class TestProjectTable:
    """Tests for the ProjectTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(ProjectTable, '__tablename__')
        assert isinstance(ProjectTable.__tablename__, str)
        assert len(ProjectTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            project_code="test_value",
            project_name="test_value",
            customer_id=MagicMock(),
            customer_name="test_value",
            start_date=date.today(),
            end_date=date.today(),
            actual_completion_date=date.today(),
            budget=Decimal("100.00"),
            actual_cost=Decimal("100.00"),
            billed_amount=Decimal("100.00"),
            paid_amount=Decimal("100.00"),
            currency="test_value",
            progress_percent=Decimal("100.00"),
            status="test_value",
            project_manager_id=MagicMock(),
            project_manager_name="test_value",
            department="test_value",
            cost_center="test_value",
            billing_type="test_value",
            contract_value=Decimal("100.00"),
            retainer_amount=Decimal("100.00"),
            description="test_value",
            notes="test_value",
            project_type="test_value",
            priority="test_value",
            parent_project_id=MagicMock(),
            created_by=MagicMock(),
            retainer_contracts=[MagicMock()],
            time_entries=[MagicMock()],
        )
        try:
            instance = ProjectTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, ProjectTable)
