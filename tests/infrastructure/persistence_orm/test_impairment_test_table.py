# AUTO-GENERATED TESTS for infrastructure/persistence_orm/impairment_test_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.impairment_test_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.impairment_test_table import ImpairmentTestTable


class TestImpairmentTestTable:
    """Tests for the ImpairmentTestTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(ImpairmentTestTable, '__tablename__')
        assert isinstance(ImpairmentTestTable.__tablename__, str)
        assert len(ImpairmentTestTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': MagicMock(),
            'asset_id': MagicMock(),
            'test_date': date.today(),
            'carrying_amount': Decimal("100.00"),
            'recoverable_amount': Decimal("100.00"),
            'impairment_loss': Decimal("100.00"),
            'currency': "test_value",
            'reason': "test_value",
            'journal_id': MagicMock(),
            'created_by': MagicMock(),
            'asset': MagicMock(),
        }
        try:
            instance = ImpairmentTestTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, ImpairmentTestTable)
