# AUTO-GENERATED TESTS for infrastructure/persistence_orm/legal_entity_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.legal_entity_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.legal_entity_table import LegalEntityTable


class TestLegalEntityTable:
    """Tests for the LegalEntityTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(LegalEntityTable, '__tablename__')
        assert isinstance(LegalEntityTable.__tablename__, str)
        assert len(LegalEntityTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            legal_name="test_value",
            trade_name="test_value",
            entity_type="test_value",
            registration_number="test_value",
            npwp="test_value",
            address="test_value",
            city="test_value",
            postal_code="test_value",
            country="test_value",
            phone="test_value",
            email="test_value",
            website="test_value",
            established_date=date.today(),
            fiscal_year_start=1,
            fiscal_year_end=1,
            base_currency="test_value",
            functional_currency="test_value",
            tax_office="test_value",
            tax_office_code="test_value",
            tax_classification="test_value",
            taxable_date=date.today(),
            annual_tax_return_due_date=1,
            monthly_tax_due_date=1,
            is_vat_collector=True,
            vat_collector_number="test_value",
            is_withholding_agent=True,
            status="test_value",
            is_active=True,
            parent_company_id=MagicMock(),
            consolidation_group_id=MagicMock(),
            logo_url="test_value",
            extra_metadata={},
            created_by=MagicMock(),
            parent=MagicMock(),
            children=[MagicMock()],
            branches=[MagicMock()],
            accounts=[MagicMock()],
            users=[MagicMock()],
            journals=[MagicMock()],
            nsfp_ranges=[MagicMock()],
            intangible_assets=[MagicMock()],
            consolidation_members=[MagicMock()],
        )
        try:
            instance = LegalEntityTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, LegalEntityTable)
