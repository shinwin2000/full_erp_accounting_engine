# AUTO-GENERATED TESTS for infrastructure/persistence_orm/bank_account_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.bank_account_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.bank_account_table import BankAccountTable


class TestBankAccountTable:
    """Tests for the BankAccountTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(BankAccountTable, '__tablename__')
        assert isinstance(BankAccountTable.__tablename__, str)
        assert len(BankAccountTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': uuid4(),
            'account_number': "test_value",
            'bank_name': "test_value",
            'bank_code': "test_value",
            'account_name': "test_value",
            'currency_code': "test_value",
            'account_type': "test_value",
            'status': "test_value",
            'is_active': True,
            'is_default': True,
            'current_balance': Decimal("100.00"),
            'available_balance': Decimal("100.00"),
            'opening_balance': Decimal("100.00"),
            'opening_balance_date': date.today(),
            'gl_account_id': MagicMock(),
            'last_reconciliation_date': date.today(),
            'created_by': MagicMock(),
            'transactions': [MagicMock()],
            'reconciliations': [MagicMock()],
        }
        try:
            instance = BankAccountTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, BankAccountTable)
