# AUTO-GENERATED TESTS for infrastructure/persistence_orm/ledger_entry_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.ledger_entry_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.ledger_entry_table import LedgerEntryTable


class TestLedgerEntryTable:
    """Tests for the LedgerEntryTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(LedgerEntryTable, '__tablename__')
        assert isinstance(LedgerEntryTable.__tablename__, str)
        assert len(LedgerEntryTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            journal_id=uuid4(),
            account_id=uuid4(),
            account_code="test_value",
            line_number=1,
            debit_amount=Decimal("100.00"),
            credit_amount=Decimal("0"),
            currency="test_value",
            posting_date=date.today(),
            cost_center="test_value",
            department="test_value",
            reference_number="test_value",
            description="test_value",
            fiscal_year=1,
            period_month=1,
            legal_entity_id=uuid4(),
            created_by=MagicMock(),
            audit_metadata={},
            journal=MagicMock(),
            account=MagicMock(),
        )
        try:
            instance = LedgerEntryTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, LedgerEntryTable)
