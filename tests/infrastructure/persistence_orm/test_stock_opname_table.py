# AUTO-GENERATED TESTS for infrastructure/persistence_orm/stock_opname_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.stock_opname_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.stock_opname_table import StockOpnameTable


class TestStockOpnameTable:
    """Tests for the StockOpnameTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(StockOpnameTable, '__tablename__')
        assert isinstance(StockOpnameTable.__tablename__, str)
        assert len(StockOpnameTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            legal_entity_id=MagicMock(),
            opname_number="test_value",
            opname_date=date.today(),
            location_code="test_value",
            warehouse_id=MagicMock(),
            description="test_value",
            status="test_value",
            total_expected_value=Decimal("100.00"),
            total_counted_value=Decimal("100.00"),
            total_variance_value=Decimal("100.00"),
            adjustment_journal_id=MagicMock(),
            completed_by=MagicMock(),
            completed_at=datetime.now(UTC),
            approved_by=MagicMock(),
            approved_at=datetime.now(UTC),
            created_by=MagicMock(),
            extra_metadata={},
            lines=[MagicMock()],
            warehouse=MagicMock(),
        )
        try:
            instance = StockOpnameTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, StockOpnameTable)
