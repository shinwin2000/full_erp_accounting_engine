# AUTO-GENERATED TESTS for infrastructure/persistence_orm/projection_checkpoint_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.projection_checkpoint_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.projection_checkpoint_table import ProjectionCheckpointTable


class TestProjectionCheckpointTable:
    """Tests for the ProjectionCheckpointTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(ProjectionCheckpointTable, '__tablename__')
        assert isinstance(ProjectionCheckpointTable.__tablename__, str)
        assert len(ProjectionCheckpointTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': uuid4(),
            'projection_name': "test_value",
            'last_event_id': MagicMock(),
            'last_event_sequence': 1,
            'last_processed_at': datetime.now(UTC),
            'processed_count': 1,
            'is_rebuilding': True,
            'rebuild_started_at': datetime.now(UTC),
            'rebuild_completed_at': datetime.now(UTC),
            'legal_entity_id': MagicMock(),
        }
        try:
            instance = ProjectionCheckpointTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, ProjectionCheckpointTable)
