# AUTO-GENERATED TESTS for infrastructure/persistence_orm/stock_opname_lines_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.stock_opname_lines_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.stock_opname_lines_table import StockOpnameLinesTable


class TestStockOpnameLinesTable:
    """Tests for the StockOpnameLinesTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(StockOpnameLinesTable, '__tablename__')
        assert isinstance(StockOpnameLinesTable.__tablename__, str)
        assert len(StockOpnameLinesTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': uuid4(),
            'opname_id': uuid4(),
            'item_id': uuid4(),
            'system_quantity': Decimal("100.00"),
            'physical_quantity': Decimal("100.00"),
            'variance': Decimal("100.00"),
            'adjustment_journal_id': uuid4(),
            'created_at': datetime.now(UTC),
        }
        try:
            instance = StockOpnameLinesTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, StockOpnameLinesTable)
