# AUTO-GENERATED TESTS for infrastructure/persistence_orm/asset_category_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.asset_category_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.asset_category_table import AssetCategoryTable


class TestAssetCategoryTable:
    """Tests for the AssetCategoryTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(AssetCategoryTable, '__tablename__')
        assert isinstance(AssetCategoryTable.__tablename__, str)
        assert len(AssetCategoryTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': uuid4(),
            'legal_entity_id': uuid4(),
            'code': "test_value",
            'name': "test_value",
            'description': "test_value",
            'default_useful_life': 1,
            'default_depreciation_method': "test_value",
            'is_active': True,
            'created_by': MagicMock(),
        }
        try:
            instance = AssetCategoryTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, AssetCategoryTable)
