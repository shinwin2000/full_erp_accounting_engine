# AUTO-GENERATED TESTS for infrastructure/persistence_orm/salary_component_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.salary_component_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.salary_component_table import SalaryComponentTable


class TestSalaryComponentTable:
    """Tests for the SalaryComponentTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(SalaryComponentTable, '__tablename__')
        assert isinstance(SalaryComponentTable.__tablename__, str)
        assert len(SalaryComponentTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            employee_id=MagicMock(),
            payroll_run_id=MagicMock(),
            component_name="test_value",
            component_type="test_value",
            calculation_type="test_value",
            amount=Decimal("100.00"),
            rate_percentage=Decimal("100.00"),
            currency="test_value",
            description="test_value",
            created_by=MagicMock(),
            employee=MagicMock(),
            payroll_run=MagicMock(),
        )
        try:
            instance = SalaryComponentTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, SalaryComponentTable)
