# AUTO-GENERATED TESTS for infrastructure/persistence_orm/goods_receipt_note_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.goods_receipt_note_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.goods_receipt_note_table import (
    GoodsReceiptNoteLineTable,
    GoodsReceiptNoteTable,
)


class TestGoodsReceiptNoteTable:
    """Tests for the GoodsReceiptNoteTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(GoodsReceiptNoteTable, '__tablename__')
        assert isinstance(GoodsReceiptNoteTable.__tablename__, str)
        assert len(GoodsReceiptNoteTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            grn_number="test_value",
            purchase_order_id=MagicMock(),
            receipt_date=date.today(),
            supplier_id=MagicMock(),
            supplier_name="test_value",
            status="test_value",
            notes="test_value",
            created_by=MagicMock(),
            created_at=datetime.now(UTC),
            updated_at=datetime.now(UTC),
            supplier=MagicMock(),
            purchase_order=MagicMock(),
            ap_invoices=[MagicMock()],
            lines=[MagicMock()],
            _events=[{}],
        )
        try:
            instance = GoodsReceiptNoteTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, GoodsReceiptNoteTable)


class TestGoodsReceiptNoteLineTable:
    """Tests for the GoodsReceiptNoteLineTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(GoodsReceiptNoteLineTable, '__tablename__')
        assert isinstance(GoodsReceiptNoteLineTable.__tablename__, str)
        assert len(GoodsReceiptNoteLineTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            grn_id=MagicMock(),
            purchase_order_line_id=MagicMock(),
            product_id=MagicMock(),
            product_code="test_value",
            product_name="test_value",
            ordered_quantity=Decimal("100.00"),
            received_quantity=Decimal("100.00"),
            unit_of_measure="test_value",
            unit_cost=Decimal("100.00"),
            total_cost=Decimal("100.00"),
            line_number=1,
            notes="test_value",
            created_at=datetime.now(UTC),
            grn=MagicMock(),
        )
        try:
            instance = GoodsReceiptNoteLineTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, GoodsReceiptNoteLineTable)
