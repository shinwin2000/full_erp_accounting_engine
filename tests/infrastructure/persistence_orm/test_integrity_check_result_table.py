# AUTO-GENERATED TESTS for infrastructure/persistence_orm/integrity_check_result_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.integrity_check_result_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.integrity_check_result_table import IntegrityCheckResultTable


class TestIntegrityCheckResultTable:
    """Tests for the IntegrityCheckResultTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(IntegrityCheckResultTable, '__tablename__')
        assert isinstance(IntegrityCheckResultTable.__tablename__, str)
        assert len(IntegrityCheckResultTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            check_started_at=datetime.now(UTC),
            check_completed_at=datetime.now(UTC),
            chain_type="test_value",
            chain_id_start=uuid4(),
            chain_id_end=uuid4(),
            total_entries_checked=1,
            total_entries_valid=1,
            total_entries_invalid=1,
            invalid_chain_ids="test_value",
            status="test_value",
            report_path="test_value",
            triggered_by=uuid4(),
        )
        try:
            instance = IntegrityCheckResultTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, IntegrityCheckResultTable)
