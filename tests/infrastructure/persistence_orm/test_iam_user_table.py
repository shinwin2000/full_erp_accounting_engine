# AUTO-GENERATED TESTS for infrastructure/persistence_orm/iam_user_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.iam_user_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.iam_user_table import (
    IAMPermissionTable,
    IAMRoleTable,
    IAMSessionTable,
    IAMUserTable,
    LoginAttemptTable,
)


class TestIAMUserTable:
    """Tests for the IAMUserTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(IAMUserTable, '__tablename__')
        assert isinstance(IAMUserTable.__tablename__, str)
        assert len(IAMUserTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            roles=[MagicMock()],
            legal_entities=[MagicMock()],
            sessions=[MagicMock()],
            login_attempts=[MagicMock()],
        )
        try:
            instance = IAMUserTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, IAMUserTable)


class TestIAMRoleTable:
    """Tests for the IAMRoleTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(IAMRoleTable, '__tablename__')
        assert isinstance(IAMRoleTable.__tablename__, str)
        assert len(IAMRoleTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            permissions=[MagicMock()],
            users=[MagicMock()],
        )
        try:
            instance = IAMRoleTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, IAMRoleTable)


class TestIAMPermissionTable:
    """Tests for the IAMPermissionTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(IAMPermissionTable, '__tablename__')
        assert isinstance(IAMPermissionTable.__tablename__, str)
        assert len(IAMPermissionTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            roles=[MagicMock()],
        )
        try:
            instance = IAMPermissionTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, IAMPermissionTable)


class TestIAMSessionTable:
    """Tests for the IAMSessionTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(IAMSessionTable, '__tablename__')
        assert isinstance(IAMSessionTable.__tablename__, str)
        assert len(IAMSessionTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            user=MagicMock(),
        )
        try:
            instance = IAMSessionTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, IAMSessionTable)


class TestLoginAttemptTable:
    """Tests for the LoginAttemptTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(LoginAttemptTable, '__tablename__')
        assert isinstance(LoginAttemptTable.__tablename__, str)
        assert len(LoginAttemptTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
        )
        try:
            instance = LoginAttemptTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, LoginAttemptTable)
