# AUTO-GENERATED TESTS for infrastructure/persistence_orm/login_attempt_orm_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.login_attempt_orm_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.login_attempt_orm_table import LoginAttemptOrmTable


class TestLoginAttemptOrmTable:
    """Tests for the LoginAttemptOrmTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(LoginAttemptOrmTable, '__tablename__')
        assert isinstance(LoginAttemptOrmTable.__tablename__, str)
        assert len(LoginAttemptOrmTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': uuid4(),
            'username': "test_value",
            'user_id': uuid4(),
            'ip_address': "test_value",
            'user_agent': "test_value",
            'success': True,
            'failure_reason': "test_value",
            'attempted_at': datetime.now(UTC),
            'request_id': "test_value",
            'created_at': datetime.now(UTC),
            'updated_at': datetime.now(UTC),
            'deleted_at': datetime.now(UTC),
        }
        try:
            instance = LoginAttemptOrmTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, LoginAttemptOrmTable)
