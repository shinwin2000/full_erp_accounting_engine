# AUTO-GENERATED TESTS for infrastructure/persistence_orm/coretax_submission_log_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.coretax_submission_log_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.coretax_submission_log_table import CoretaxSubmissionLogTable


class TestCoretaxSubmissionLogTable:
    """Tests for the CoretaxSubmissionLogTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(CoretaxSubmissionLogTable, '__tablename__')
        assert isinstance(CoretaxSubmissionLogTable.__tablename__, str)
        assert len(CoretaxSubmissionLogTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            submission_id=MagicMock(),
            spt_type="test_value",
            npwp="test_value",
            action="test_value",
            status="test_value",
            request_payload={},
            response_payload={},
            error_message="test_value",
            created_at=datetime.now(UTC),
        )
        try:
            instance = CoretaxSubmissionLogTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, CoretaxSubmissionLogTable)
