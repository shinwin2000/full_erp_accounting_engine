# AUTO-GENERATED TESTS for infrastructure/persistence_orm/retainer_contract_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.retainer_contract_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.retainer_contract_table import RetainerContractTable


class TestRetainerContractTable:
    """Tests for the RetainerContractTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(RetainerContractTable, '__tablename__')
        assert isinstance(RetainerContractTable.__tablename__, str)
        assert len(RetainerContractTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            contract_number="test_value",
            contract_name="test_value",
            customer_id=MagicMock(),
            project_id=MagicMock(),
            contract_value=Decimal("100.00"),
            remaining_amount=Decimal("100.00"),
            billed_amount=Decimal("100.00"),
            used_amount=Decimal("100.00"),
            currency="test_value",
            start_date=date.today(),
            end_date=date.today(),
            next_billing_date=date.today(),
            last_billing_date=date.today(),
            billing_frequency="test_value",
            billing_amount=Decimal("100.00"),
            auto_billing=True,
            service_description="test_value",
            terms_and_conditions="test_value",
            max_hours_per_month=Decimal("100.00"),
            hourly_rate=Decimal("100.00"),
            status="test_value",
            approved_by=MagicMock(),
            approved_at=datetime.now(UTC),
            notes="test_value",
            created_by=MagicMock(),
            customer=MagicMock(),
            project=MagicMock(),
        )
        try:
            instance = RetainerContractTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, RetainerContractTable)
