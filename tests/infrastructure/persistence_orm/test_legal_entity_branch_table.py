# AUTO-GENERATED TESTS for infrastructure/persistence_orm/legal_entity_branch_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.legal_entity_branch_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.legal_entity_branch_table import LegalEntityBranchTable


class TestLegalEntityBranchTable:
    """Tests for the LegalEntityBranchTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(LegalEntityBranchTable, '__tablename__')
        assert isinstance(LegalEntityBranchTable.__tablename__, str)
        assert len(LegalEntityBranchTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': MagicMock(),
            'parent_entity_id': MagicMock(),
            'branch_name': "test_value",
            'branch_code': "test_value",
            'address': "test_value",
            'city': "test_value",
            'phone': "test_value",
            'manager_name': "test_value",
            'is_active': True,
            'created_by': MagicMock(),
            'created_at': datetime.now(UTC),
            'updated_at': datetime.now(UTC),
            'deleted_at': datetime.now(UTC),
            'parent_entity': MagicMock(),
        }
        try:
            instance = LegalEntityBranchTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, LegalEntityBranchTable)
