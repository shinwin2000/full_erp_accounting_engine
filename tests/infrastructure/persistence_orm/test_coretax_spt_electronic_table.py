# AUTO-GENERATED TESTS for infrastructure/persistence_orm/coretax_spt_electronic_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.coretax_spt_electronic_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.coretax_spt_electronic_table import CoretaxSptElectronicTable


class TestCoretaxSptElectronicTable:
    """Tests for the CoretaxSptElectronicTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(CoretaxSptElectronicTable, '__tablename__')
        assert isinstance(CoretaxSptElectronicTable.__tablename__, str)
        assert len(CoretaxSptElectronicTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': uuid4(),
            'legal_entity_id': uuid4(),
            'spt_type': "test_value",
            'tax_period_month': 1,
            'tax_period_year': 1,
            'spt_number': "test_value",
            'spt_version': 1,
            'status': "test_value",
            'submitted_at': datetime.now(UTC),
            'approved_at': datetime.now(UTC),
            'rejection_reason': "test_value",
            'payment_amount': Decimal("100.00"),
            'payment_reference': "test_value",
            'ntpn': "test_value",
            'xml_content': "test_value",
            'pdf_attachment': "test_value",
            'signed_by': uuid4(),
            'signed_at': datetime.now(UTC),
            'digital_signature': "test_value",
            'is_amendment': True,
            'original_spt_id': uuid4(),
            'created_at': datetime.now(UTC),
            'created_by': uuid4(),
            'updated_at': datetime.now(UTC),
            'updated_by': uuid4(),
        }
        try:
            instance = CoretaxSptElectronicTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, CoretaxSptElectronicTable)
