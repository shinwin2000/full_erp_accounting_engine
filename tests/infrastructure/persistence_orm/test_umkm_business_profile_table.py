# AUTO-GENERATED TESTS for infrastructure/persistence_orm/umkm_business_profile_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.umkm_business_profile_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.umkm_business_profile_table import UMKMProfileTable


class TestUMKMProfileTable:
    """Tests for the UMKMProfileTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(UMKMProfileTable, '__tablename__')
        assert isinstance(UMKMProfileTable.__tablename__, str)
        assert len(UMKMProfileTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            business_name="test_value",
            business_type="test_value",
            taxpayer_npwp="test_value",
            business_address="test_value",
            uses_umkm_tax=True,
            tax_method="test_value",
            annual_revenue_threshold=1,
            extra_metadata={},
            created_by=MagicMock(),
            updated_by=MagicMock(),
        )
        try:
            instance = UMKMProfileTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, UMKMProfileTable)
