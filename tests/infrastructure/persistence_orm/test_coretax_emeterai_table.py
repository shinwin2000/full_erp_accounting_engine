# AUTO-GENERATED TESTS for infrastructure/persistence_orm/coretax_emeterai_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.coretax_emeterai_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.coretax_emeterai_table import CoretaxEMeteraiTable


class TestCoretaxEMeteraiTable:
    """Tests for the CoretaxEMeteraiTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(CoretaxEMeteraiTable, '__tablename__')
        assert isinstance(CoretaxEMeteraiTable.__tablename__, str)
        assert len(CoretaxEMeteraiTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': MagicMock(),
            'meterai_code': "test_value",
            'npwp': "test_value",
            'value': Decimal("100.00"),
            'status': "test_value",
            'purchase_date': date.today(),
            'purchase_transaction_id': "test_value",
            'used_at': datetime.now(UTC),
            'used_on_document': "test_value",
            'used_by': MagicMock(),
        }
        try:
            instance = CoretaxEMeteraiTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, CoretaxEMeteraiTable)
