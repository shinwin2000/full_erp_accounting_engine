# AUTO-GENERATED TESTS for infrastructure/persistence_orm/budget_actual_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.budget_actual_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.budget_actual_table import BudgetActualTable


class TestBudgetActualTable:
    """Tests for the BudgetActualTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(BudgetActualTable, '__tablename__')
        assert isinstance(BudgetActualTable.__tablename__, str)
        assert len(BudgetActualTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            budget_id=uuid4(),
            transaction_date=date.today(),
            amount=Decimal("100.00"),
            currency="test_value",
            source_type="test_value",
            source_id=uuid4(),
            source_number="test_value",
            description="test_value",
            reference="test_value",
            cost_center="test_value",
            project_id=MagicMock(),
            status="test_value",
            reversed_by_id=MagicMock(),
            reversed_at=datetime.now(UTC),
            created_by=MagicMock(),
            budget=MagicMock(),
        )
        try:
            instance = BudgetActualTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, BudgetActualTable)
