# AUTO-GENERATED TESTS for infrastructure/persistence_orm/fair_value_hierarchy_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.fair_value_hierarchy_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.fair_value_hierarchy_table import FairValueHierarchyTable


class TestFairValueHierarchyTable:
    """Tests for the FairValueHierarchyTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(FairValueHierarchyTable, '__tablename__')
        assert isinstance(FairValueHierarchyTable.__tablename__, str)
        assert len(FairValueHierarchyTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            financial_instrument_id=uuid4(),
            instrument_type="test_value",
            valuation_date=date.today(),
            fair_value=Decimal("100.00"),
            level_input=1,
            level_description="test_value",
            unobservable_inputs="test_value",
            sensitivity_analysis="test_value",
            valuer_name="test_value",
            valuation_report_path="test_value",
            created_at=datetime.now(UTC),
        )
        try:
            instance = FairValueHierarchyTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, FairValueHierarchyTable)
