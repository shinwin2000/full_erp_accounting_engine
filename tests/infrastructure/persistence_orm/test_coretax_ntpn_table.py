# AUTO-GENERATED TESTS for infrastructure/persistence_orm/coretax_ntpn_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.coretax_ntpn_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.coretax_ntpn_table import CoretaxNTPNTable


class TestCoretaxNTPNTable:
    """Tests for the CoretaxNTPNTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(CoretaxNTPNTable, '__tablename__')
        assert isinstance(CoretaxNTPNTable.__tablename__, str)
        assert len(CoretaxNTPNTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': uuid4(),
            'ntpn': "test_value",
            'amount': Decimal("100.00"),
            'payment_date': date.today(),
            'npwp': "test_value",
            'is_valid': True,
            'validation_result': {},
            'validated_at': datetime.now(UTC),
        }
        try:
            instance = CoretaxNTPNTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, CoretaxNTPNTable)
