# AUTO-GENERATED TESTS for infrastructure/persistence_orm/budget_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.budget_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.budget_table import BudgetTable


class TestBudgetTable:
    """Tests for the BudgetTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(BudgetTable, '__tablename__')
        assert isinstance(BudgetTable.__tablename__, str)
        assert len(BudgetTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            budget_code="test_value",
            budget_name="test_value",
            description="test_value",
            budget_type="test_value",
            fiscal_year=1,
            period=1,
            start_date=date.today(),
            end_date=date.today(),
            account_code="test_value",
            account_name="test_value",
            cost_center="test_value",
            project_id=MagicMock(),
            department="test_value",
            amount=Decimal("100.00"),
            currency="test_value",
            status="test_value",
            is_active=True,
            submitted_by=MagicMock(),
            submitted_at=datetime.now(UTC),
            approved_by=MagicMock(),
            approved_at=datetime.now(UTC),
            rejection_reason="test_value",
            original_budget_id=MagicMock(),
            revision_number=1,
            created_by=MagicMock(),
            updated_by=MagicMock(),
            actuals=[MagicMock()],
        )
        try:
            instance = BudgetTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, BudgetTable)
