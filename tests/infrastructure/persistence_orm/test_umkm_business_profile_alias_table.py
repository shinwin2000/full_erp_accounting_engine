# AUTO-GENERATED TESTS for infrastructure/persistence_orm/umkm_business_profile_alias_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.umkm_business_profile_alias_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.umkm_business_profile_alias_table import (
    UmkmBusinessProfileTable,
)


class TestUmkmBusinessProfileTable:
    """Tests for the UmkmBusinessProfileTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(UmkmBusinessProfileTable, '__tablename__')
        assert isinstance(UmkmBusinessProfileTable.__tablename__, str)
        assert len(UmkmBusinessProfileTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            legal_entity_id=uuid4(),
            business_type="test_value",
            annual_turnover=Decimal("100.00"),
            uses_simplified_journal=True,
            created_at=datetime.now(UTC),
            updated_at=datetime.now(UTC),
            created_by=uuid4(),
            updated_by=uuid4(),
        )
        try:
            instance = UmkmBusinessProfileTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, UmkmBusinessProfileTable)
