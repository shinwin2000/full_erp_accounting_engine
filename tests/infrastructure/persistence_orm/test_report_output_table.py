# AUTO-GENERATED TESTS for infrastructure/persistence_orm/report_output_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.report_output_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.report_output_table import ReportOutputTable


class TestReportOutputTable:
    """Tests for the ReportOutputTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(ReportOutputTable, '__tablename__')
        assert isinstance(ReportOutputTable.__tablename__, str)
        assert len(ReportOutputTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            definition_id=uuid4(),
            output_format="test_value",
            file_path="test_value",
            file_size_bytes=1,
            parameters={},
            generated_at=datetime.now(UTC),
            generated_by=MagicMock(),
            status="test_value",
            error_message="test_value",
        )
        try:
            instance = ReportOutputTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, ReportOutputTable)
