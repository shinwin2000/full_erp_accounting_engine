# AUTO-GENERATED TESTS for infrastructure/persistence_orm/revaluation_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.revaluation_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import date
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.revaluation_table import RevaluationTable


class TestRevaluationTable:
    """Tests for the RevaluationTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(RevaluationTable, '__tablename__')
        assert isinstance(RevaluationTable.__tablename__, str)
        assert len(RevaluationTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            asset_id=MagicMock(),
            revaluation_date=date.today(),
            old_acquisition_cost=Decimal("100.00"),
            new_acquisition_cost=Decimal("100.00"),
            old_accumulated_depreciation=Decimal("100.00"),
            new_accumulated_depreciation=Decimal("100.00"),
            old_nbv=Decimal("100.00"),
            new_nbv=Decimal("100.00"),
            surplus_deficit=Decimal("100.00"),
            currency="test_value",
            reason="test_value",
            journal_id=MagicMock(),
            created_by=MagicMock(),
            asset=MagicMock(),
        )
        try:
            instance = RevaluationTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, RevaluationTable)
