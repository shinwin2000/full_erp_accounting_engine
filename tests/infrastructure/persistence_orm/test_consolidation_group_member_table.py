# AUTO-GENERATED TESTS for infrastructure/persistence_orm/consolidation_group_member_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.consolidation_group_member_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.consolidation_group_member_table import (
    ConsolidationGroupMemberTable,
)


class TestConsolidationGroupMemberTable:
    """Tests for the ConsolidationGroupMemberTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(ConsolidationGroupMemberTable, '__tablename__')
        assert isinstance(ConsolidationGroupMemberTable.__tablename__, str)
        assert len(ConsolidationGroupMemberTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            group_id=MagicMock(),
            entity_id=MagicMock(),
            ownership_percentage=Decimal("100.00"),
            joined_at=datetime.now(UTC),
            group=MagicMock(),
        )
        try:
            instance = ConsolidationGroupMemberTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, ConsolidationGroupMemberTable)
