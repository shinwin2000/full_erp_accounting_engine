# AUTO-GENERATED TESTS for infrastructure/persistence_orm/hash_chain_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.hash_chain_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.hash_chain_table import HashChainTable


class TestHashChainTable:
    """Tests for the HashChainTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(HashChainTable, '__tablename__')
        assert isinstance(HashChainTable.__tablename__, str)
        assert len(HashChainTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            stream_name="test_value",
            last_hash="test_value",
            last_sequence=1,
            event_count=1,
            genesis_hash="test_value",
            status="test_value",
            last_verified_at=datetime.now(UTC),
            verified_by=MagicMock(),
            broken_at_sequence=1,
            repair_history={},
        )
        try:
            instance = HashChainTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, HashChainTable)
