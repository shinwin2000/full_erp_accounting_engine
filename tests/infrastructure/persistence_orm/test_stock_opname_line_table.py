# AUTO-GENERATED TESTS for infrastructure/persistence_orm/stock_opname_line_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.stock_opname_line_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.stock_opname_line_table import StockOpnameLineTable


class TestStockOpnameLineTable:
    """Tests for the StockOpnameLineTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(StockOpnameLineTable, '__tablename__')
        assert isinstance(StockOpnameLineTable.__tablename__, str)
        assert len(StockOpnameLineTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            stock_opname_id=MagicMock(),
            product_id=MagicMock(),
            product_code="test_value",
            product_name="test_value",
            warehouse_id=MagicMock(),
            system_quantity=Decimal("100.00"),
            physical_quantity=Decimal("100.00"),
            difference_quantity=Decimal("100.00"),
            unit_cost=Decimal("100.00"),
            difference_value=Decimal("100.00"),
            adjustment_journal_id=MagicMock(),
            notes="test_value",
            stock_opname=MagicMock(),
        )
        try:
            instance = StockOpnameLineTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, StockOpnameLineTable)
