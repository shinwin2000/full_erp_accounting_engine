# AUTO-GENERATED TESTS for infrastructure/persistence_orm/consolidation_group_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.consolidation_group_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.consolidation_group_table import ConsolidationGroupTable


class TestConsolidationGroupTable:
    """Tests for the ConsolidationGroupTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(ConsolidationGroupTable, '__tablename__')
        assert isinstance(ConsolidationGroupTable.__tablename__, str)
        assert len(ConsolidationGroupTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': MagicMock(),
            'group_name': "test_value",
            'description': "test_value",
            'is_active': True,
            'created_by': MagicMock(),
            'members': [MagicMock()],
        }
        try:
            instance = ConsolidationGroupTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, ConsolidationGroupTable)
