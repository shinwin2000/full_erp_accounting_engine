# AUTO-GENERATED TESTS for infrastructure/persistence_orm/ledger_entry_template_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.ledger_entry_template_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from uuid import uuid4

import pytest

from infrastructure.persistence_orm.ledger_entry_template_table import LedgerEntryTemplateTable


class TestLedgerEntryTemplateTable:
    """Tests for the LedgerEntryTemplateTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(LedgerEntryTemplateTable, '__tablename__')
        assert isinstance(LedgerEntryTemplateTable.__tablename__, str)
        assert len(LedgerEntryTemplateTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
        )
        try:
            instance = LedgerEntryTemplateTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, LedgerEntryTemplateTable)
