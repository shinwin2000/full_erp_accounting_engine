# AUTO-GENERATED TESTS for infrastructure/persistence_orm/outbox_relay_metrics_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.outbox_relay_metrics_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.outbox_relay_metrics_table import OutboxRelayMetricsTable


class TestOutboxRelayMetricsTable:
    """Tests for the OutboxRelayMetricsTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(OutboxRelayMetricsTable, '__tablename__')
        assert isinstance(OutboxRelayMetricsTable.__tablename__, str)
        assert len(OutboxRelayMetricsTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            consumer_group="test_value",
            poll_interval_ms=1,
            batch_size=1,
            processing_latency_avg_ms=1,
            events_per_second=Decimal("100.00"),
            queue_depth=1,
            last_poll_at=datetime.now(UTC),
            created_at=datetime.now(UTC),
        )
        try:
            instance = OutboxRelayMetricsTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, OutboxRelayMetricsTable)
