# AUTO-GENERATED TESTS for infrastructure/persistence_orm/outbox_relay_checkpoint_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.outbox_relay_checkpoint_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.outbox_relay_checkpoint_table import OutboxRelayCheckpointTable


class TestOutboxRelayCheckpointTable:
    """Tests for the OutboxRelayCheckpointTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(OutboxRelayCheckpointTable, '__tablename__')
        assert isinstance(OutboxRelayCheckpointTable.__tablename__, str)
        assert len(OutboxRelayCheckpointTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            consumer_group="test_value",
            last_processed_id=uuid4(),
            last_processed_at=datetime.now(UTC),
            processed_by="test_value",
        )
        try:
            instance = OutboxRelayCheckpointTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, OutboxRelayCheckpointTable)
