# AUTO-GENERATED TESTS for infrastructure/persistence_orm/payslip_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.payslip_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.payslip_table import PayslipTable


class TestPayslipTable:
    """Tests for the PayslipTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(PayslipTable, '__tablename__')
        assert isinstance(PayslipTable.__tablename__, str)
        assert len(PayslipTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=MagicMock(),
            payslip_number="test_value",
            period_year=1,
            period_month=1,
            employee_id=MagicMock(),
            employee_code="test_value",
            employee_name="test_value",
            position="test_value",
            department="test_value",
            employment_status="test_value",
            payroll_run_id=MagicMock(),
            basic_salary=Decimal("100.00"),
            allowances=Decimal("100.00"),
            overtime=Decimal("100.00"),
            bonus=Decimal("100.00"),
            thirteenth_month=Decimal("100.00"),
            other_income=Decimal("100.00"),
            tax_pph21=Decimal("100.00"),
            bpjs_employment=Decimal("100.00"),
            bpjs_health=Decimal("100.00"),
            bpjs_pension=Decimal("100.00"),
            loan_deduction=Decimal("100.00"),
            cooperative_deduction=Decimal("100.00"),
            other_deductions=Decimal("100.00"),
            gross_income=Decimal("100.00"),
            total_deductions=Decimal("100.00"),
            net_pay=Decimal("100.00"),
            currency="test_value",
            payment_date=date.today(),
            bank_account="test_value",
            payment_reference="test_value",
            payment_run_id=MagicMock(),
            status="test_value",
            approved_by=MagicMock(),
            approved_at=datetime.now(UTC),
            earnings_breakdown={},
            deductions_breakdown={},
            tax_calculation_detail={},
            notes="test_value",
            created_by=MagicMock(),
            employee=MagicMock(),
            payroll_run=MagicMock(),
        )
        try:
            instance = PayslipTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, PayslipTable)
