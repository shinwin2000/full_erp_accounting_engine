# AUTO-GENERATED TESTS for infrastructure/persistence_orm/asset_category_alias_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.asset_category_alias_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.asset_category_alias_table import AssetCategorySingularTable


class TestAssetCategorySingularTable:
    """Tests for the AssetCategorySingularTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(AssetCategorySingularTable, '__tablename__')
        assert isinstance(AssetCategorySingularTable.__tablename__, str)
        assert len(AssetCategorySingularTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            legal_entity_id=uuid4(),
            category_code="test_value",
            category_name="test_value",
            useful_life_years=1,
            depreciation_method="test_value",
            salvage_value_percent=Decimal("100.00"),
            created_at=datetime.now(UTC),
            updated_at=datetime.now(UTC),
            created_by=uuid4(),
            updated_by=uuid4(),
        )
        try:
            instance = AssetCategorySingularTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, AssetCategorySingularTable)
