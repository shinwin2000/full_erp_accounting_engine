# AUTO-GENERATED TESTS for infrastructure/persistence_orm/event_store_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.event_store_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from infrastructure.persistence_orm.event_store_table import EventStoreTable


class TestEventStoreTable:
    """Tests for the EventStoreTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(EventStoreTable, '__tablename__')
        assert isinstance(EventStoreTable.__tablename__, str)
        assert len(EventStoreTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = dict(
            id=uuid4(),
            stream_name="test_value",
            sequence_number=1,
            event_type="test_value",
            event_version=1,
            data={},
            event_metadata={},
            timestamp=datetime.now(UTC),
            previous_hash="test_value",
            hash="test_value",
            aggregate_id=MagicMock(),
            aggregate_type="test_value",
            correlation_id="test_value",
            causation_id="test_value",
            legal_entity_id=MagicMock(),
            user_id=MagicMock(),
        )
        try:
            instance = EventStoreTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, EventStoreTable)
