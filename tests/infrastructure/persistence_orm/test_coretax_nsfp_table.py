# AUTO-GENERATED TESTS for infrastructure/persistence_orm/coretax_nsfp_table.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: infrastructure.persistence_orm.coretax_nsfp_table
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from unittest.mock import MagicMock

import pytest

from infrastructure.persistence_orm.coretax_nsfp_table import CoretaxNSFPTable, NSFStatus


class TestNSFStatus:
    """Tests for the NSFStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(NSFStatus, 'ACTIVE')
        assert hasattr(NSFStatus, 'PARTIALLY_USED')
        assert hasattr(NSFStatus, 'EXHAUSTED')
        assert hasattr(NSFStatus, 'EXPIRED')
        assert hasattr(NSFStatus, 'CANCELLED')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(NSFStatus.ACTIVE, NSFStatus)


class TestCoretaxNSFPTable:
    """Tests for the CoretaxNSFPTable ORM table model."""

    def test_tablename_defined(self):
        """ORM model declares a table name."""
        assert hasattr(CoretaxNSFPTable, '__tablename__')
        assert isinstance(CoretaxNSFPTable.__tablename__, str)
        assert len(CoretaxNSFPTable.__tablename__) > 0

    def test_instantiation(self):
        """ORM model can be instantiated in-memory (without a DB session)."""
        kwargs = {
            'id': MagicMock(),
            'start_number': 1,
            'end_number': 1,
            'current_number': 1,
            'status': NSFStatus.ACTIVE,
            'request_id': "test_value",
            'approval_code': "test_value",
            'issued_date': date.today(),
            'expiry_date': date.today(),
            'legal_entity_id': MagicMock(),
            'used_count': 1,
            'cancelled_by': MagicMock(),
            'cancelled_at': datetime.now(UTC),
            'cancellation_reason': "test_value",
        }
        try:
            instance = CoretaxNSFPTable(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"ORM instantiation needs a live session/mapper config: {e}")
            return
        assert isinstance(instance, CoretaxNSFPTable)
