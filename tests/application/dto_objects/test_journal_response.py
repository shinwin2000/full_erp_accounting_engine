# AUTO-GENERATED TESTS for application/dto_objects/journal_response.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: application.dto_objects.journal_response
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from application.dto_objects.journal_response import (
    JournalEntryResponseDTO,
    JournalLineResponseDTO,
    JournalValidationResultDTO,
)


class TestJournalLineResponseDTO:
    """Tests for the JournalLineResponseDTO value object / model."""

    def _build_kwargs(self):
        return dict(
            account_code="test_value",
            description="test_value",
            debit=Decimal("100.00"),
            credit=Decimal("0"),
            cost_center="test_value",
            department="test_value",
            project_code="test_value",
        )

    def test_construction_success(self):
        """JournalLineResponseDTO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = JournalLineResponseDTO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, JournalLineResponseDTO)
        assert instance.account_code == kwargs['account_code']


class TestJournalEntryResponseDTO:
    """Tests for the JournalEntryResponseDTO value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            journal_number="test_value",
            journal_date=datetime.now(UTC),
            period="test_value",
            description="test_value",
            lines=[MagicMock()],
            total_debit=Decimal("100.00"),
            total_credit=Decimal("0"),
            status="test_value",
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            approved_at=datetime.now(UTC),
            approved_by=uuid4(),
            reversed_at=datetime.now(UTC),
            reversed_by=uuid4(),
            original_journal_id=uuid4(),
            posted_at=datetime.now(UTC),
            posted_by=uuid4(),
            version=1,
        )

    def test_construction_success(self):
        """JournalEntryResponseDTO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = JournalEntryResponseDTO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, JournalEntryResponseDTO)
        assert instance.id == kwargs['id']


class TestJournalValidationResultDTO:
    """Tests for the JournalValidationResultDTO value object / model."""

    def _build_kwargs(self):
        return dict(
            is_valid=True,
            errors=["test_value"],
            warnings=["test_value"],
            total_debit=Decimal("100.00"),
            total_credit=Decimal("0"),
        )

    def test_construction_success(self):
        """JournalValidationResultDTO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = JournalValidationResultDTO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, JournalValidationResultDTO)
        assert instance.is_valid == kwargs['is_valid']
