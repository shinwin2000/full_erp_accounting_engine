# AUTO-GENERATED TESTS for application/sagas/coretax_submission_saga.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: application.sagas.coretax_submission_saga
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from unittest.mock import MagicMock

import pytest

from application.sagas.coretax_submission_saga import CoretaxSubmissionSaga


class TestCoretaxSubmissionSaga:
    """Tests for CoretaxSubmissionSaga."""

    def _build_instance(self):
        return CoretaxSubmissionSaga(state_store=MagicMock(), coretax_service=MagicMock())

    def test_construction(self):
        """CoretaxSubmissionSaga can be instantiated with mocked dependencies."""
        try:
            instance = self._build_instance()
        except (Exception, SystemExit) as e:
            pytest.skip(f"Requires domain-specific construction setup: {e}")
            return
        assert isinstance(instance, CoretaxSubmissionSaga)
