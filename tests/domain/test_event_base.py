# AUTO-GENERATED TESTS for domain/event_base.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: domain.event_base
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from uuid import uuid4

import pytest

from domain.event_base import DomainEvent, IntegrationEvent


class TestDomainEvent:
    """Tests for the DomainEvent value object / model."""

    def _build_kwargs(self):
        return {
            "event_id": uuid4(),
            "event_type": "test_value",
            "occurred_at": datetime.now(UTC),
            "version": 1,
        }

    def test_construction_success(self):
        """DomainEvent can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = DomainEvent(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, DomainEvent)
        assert instance.event_id == kwargs['event_id']


class TestIntegrationEvent:
    """Tests for the IntegrationEvent value object / model."""

    def _build_kwargs(self):
        return {
            "correlation_id": "test_value",
            "causation_id": "test_value",
            "user_id": uuid4(),
            "tenant_id": "test_value",
            "source_system": "test_value",
        }

    def test_construction_success(self):
        """IntegrationEvent can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = IntegrationEvent(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, IntegrationEvent)
        assert instance.correlation_id == kwargs['correlation_id']
