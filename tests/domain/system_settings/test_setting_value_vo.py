# AUTO-GENERATED TESTS for domain/system_settings/setting_value_vo.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: domain.system_settings.setting_value_vo
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from unittest.mock import MagicMock

import pytest

from domain.system_settings.setting_value_vo import SettingDataType, SettingValueVO


class TestSettingDataType:
    """Tests for the SettingDataType enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(SettingDataType, 'STRING')
        assert hasattr(SettingDataType, 'INTEGER')
        assert hasattr(SettingDataType, 'DECIMAL')
        assert hasattr(SettingDataType, 'BOOLEAN')
        assert hasattr(SettingDataType, 'JSON')
        assert hasattr(SettingDataType, 'DATE')
        assert hasattr(SettingDataType, 'DATETIME')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(SettingDataType.STRING, SettingDataType)


class TestSettingValueVO:
    """Tests for the SettingValueVO value object / model."""

    def _build_kwargs(self):
        return dict(
            value=MagicMock(),
            data_type=SettingDataType.STRING,
            set_by="test_value",
            set_at=datetime.now(UTC),
        )

    def test_construction_success(self):
        """SettingValueVO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = SettingValueVO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, SettingValueVO)
        assert instance.value == kwargs['value']
