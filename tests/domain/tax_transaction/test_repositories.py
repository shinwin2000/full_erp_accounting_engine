# AUTO-GENERATED TESTS for domain/tax_transaction/repositories.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: domain.tax_transaction.repositories
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).


from domain.tax_transaction.repositories import (
    BupotRepository,
    EMeteraiRepository,
    FakturPajakRepository,
    SPTRepository,
)


class TestFakturPajakRepository:
    """Tests for FakturPajakRepository."""

    def test_class_defined(self):
        """FakturPajakRepository is an abstract base / protocol and is importable."""
        assert FakturPajakRepository is not None


class TestSPTRepository:
    """Tests for SPTRepository."""

    def test_class_defined(self):
        """SPTRepository is an abstract base / protocol and is importable."""
        assert SPTRepository is not None


class TestBupotRepository:
    """Tests for BupotRepository."""

    def test_class_defined(self):
        """BupotRepository is an abstract base / protocol and is importable."""
        assert BupotRepository is not None


class TestEMeteraiRepository:
    """Tests for EMeteraiRepository."""

    def test_class_defined(self):
        """EMeteraiRepository is an abstract base / protocol and is importable."""
        assert EMeteraiRepository is not None
