# AUTO-GENERATED TESTS for domain/umkm_simplified/value_objects.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: domain.umkm_simplified.value_objects
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).


import pytest

from domain.umkm_simplified.value_objects import CategoryVO, PeriodVO


class TestCategoryVO:
    """Tests for the CategoryVO value object / model."""

    def _build_kwargs(self):
        return dict(
            category="test_value",
        )

    def test_construction_success(self):
        """CategoryVO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = CategoryVO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, CategoryVO)
        assert instance.category == kwargs['category']


class TestPeriodVO:
    """Tests for the PeriodVO value object / model."""

    def _build_kwargs(self):
        return dict(
            tahun=1,
            bulan=1,
        )

    def test_construction_success(self):
        """PeriodVO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = PeriodVO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, PeriodVO)
        assert instance.tahun == kwargs['tahun']
