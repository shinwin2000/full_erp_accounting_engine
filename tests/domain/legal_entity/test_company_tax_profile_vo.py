# AUTO-GENERATED TESTS for domain/legal_entity/company_tax_profile_vo.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: domain.legal_entity.company_tax_profile_vo
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from unittest.mock import MagicMock

import pytest

from domain.legal_entity.company_tax_profile_vo import (
    CompanyTaxProfileVO,
    TaxPaymentMethod,
    TaxRegime,
)


class TestTaxRegime:
    """Tests for the TaxRegime enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(TaxRegime, 'GENERAL')
        assert hasattr(TaxRegime, 'FINAL')
        assert hasattr(TaxRegime, 'GROSS_UP')
        assert hasattr(TaxRegime, 'WITHHOLDING')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(TaxRegime.GENERAL, TaxRegime)


class TestTaxPaymentMethod:
    """Tests for the TaxPaymentMethod enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(TaxPaymentMethod, 'MONTHLY_INSTALLMENT')
        assert hasattr(TaxPaymentMethod, 'ANNUAL_LUMP_SUM')
        assert hasattr(TaxPaymentMethod, 'WITHHOLDING')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(TaxPaymentMethod.MONTHLY_INSTALLMENT, TaxPaymentMethod)


class TestCompanyTaxProfileVO:
    """Tests for the CompanyTaxProfileVO value object / model."""

    def _build_kwargs(self):
        return dict(
            is_pkp=True,
            tax_regime=TaxRegime.GENERAL,
            corporate_income_tax_rate=MagicMock(),
            vat_rate=MagicMock(),
            vat_collection_method="test_value",
            income_tax_article="test_value",
            tax_bracket="test_value",
            payment_method=TaxPaymentMethod.MONTHLY_INSTALLMENT,
            annual_return_deadline_month=1,
        )

    def test_construction_success(self):
        """CompanyTaxProfileVO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = CompanyTaxProfileVO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, CompanyTaxProfileVO)
        assert instance.is_pkp == kwargs['is_pkp']
