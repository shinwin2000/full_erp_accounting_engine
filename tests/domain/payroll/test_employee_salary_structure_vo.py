# AUTO-GENERATED TESTS for domain/payroll/employee_salary_structure_vo.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: domain.payroll.employee_salary_structure_vo
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, datetime
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from domain.payroll.employee_salary_structure_vo import EmployeeSalaryStructureVO


class TestEmployeeSalaryStructureVO:
    """Tests for the EmployeeSalaryStructureVO value object / model."""

    def _build_kwargs(self):
        return dict(
            structure_id=uuid4(),
            employee_id=uuid4(),
            employee_name="test_value",
            legal_entity_id=uuid4(),
            basic_salary=Decimal("100.00"),
            currency="test_value",
            salary_components=[MagicMock()],
            ptkp_status=MagicMock(),
            bpjs_employment=MagicMock(),
            employee_nik="test_value",
            employee_position="test_value",
            bank_account_number="test_value",
            bank_account_name="test_value",
            bank_code="test_value",
            effective_date=datetime.now(UTC),
            notes="test_value",
            created_at=datetime.now(UTC),
            updated_at=datetime.now(UTC),
            created_by="test_value",
            version=1,
        )

    def test_construction_success(self):
        """EmployeeSalaryStructureVO can be constructed with valid field values."""
        kwargs = self._build_kwargs()
        try:
            instance = EmployeeSalaryStructureVO(**kwargs)
        except (Exception, SystemExit) as e:
            pytest.skip(f"Domain validation rejected generic dummy data (needs realistic fixture): {e}")
            return
        assert isinstance(instance, EmployeeSalaryStructureVO)
        assert instance.structure_id == kwargs['structure_id']
