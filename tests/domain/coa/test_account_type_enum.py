# AUTO-GENERATED TESTS for domain/coa/account_type_enum.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: domain.coa.account_type_enum
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).


import pytest

from domain.coa.account_type_enum import (
    AccountType,
    get_account_type_display,
    get_normal_balance_from_type,
    is_valid_account_type,
)


class TestAccountType:
    """Tests for the AccountType enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(AccountType, 'ASSET')
        assert hasattr(AccountType, 'LIABILITY')
        assert hasattr(AccountType, 'EQUITY')
        assert hasattr(AccountType, 'REVENUE')
        assert hasattr(AccountType, 'EXPENSE')
        assert hasattr(AccountType, 'CONTRA_ASSET')
        assert hasattr(AccountType, 'CONTRA_LIABILITY')
        assert hasattr(AccountType, 'CONTRA_EQUITY')
        assert hasattr(AccountType, 'CONTRA_REVENUE')
        assert hasattr(AccountType, 'CONTRA_EXPENSE')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(AccountType.ASSET, AccountType)


def test_is_valid_account_type_smoke():
    """Smoke test for module-level function is_valid_account_type."""
    try:
        result = is_valid_account_type(type_str="test_value")
    except (Exception, SystemExit) as e:
        pytest.skip(f"is_valid_account_type needs specific input data: {e}")
        return
    assert True


def test_get_account_type_display_smoke():
    """Smoke test for module-level function get_account_type_display."""
    try:
        result = get_account_type_display(type_str=AccountType.ASSET)
    except (Exception, SystemExit) as e:
        pytest.skip(f"get_account_type_display needs specific input data: {e}")
        return
    assert True


def test_get_normal_balance_from_type_smoke():
    """Smoke test for module-level function get_normal_balance_from_type."""
    try:
        result = get_normal_balance_from_type(account_type=AccountType.ASSET)
    except (Exception, SystemExit) as e:
        pytest.skip(f"get_normal_balance_from_type needs specific input data: {e}")
        return
    assert True
