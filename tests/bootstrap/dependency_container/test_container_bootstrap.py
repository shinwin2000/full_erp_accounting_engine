# AUTO-GENERATED TESTS for bootstrap/dependency_container/container_bootstrap.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: bootstrap.dependency_container.container_bootstrap
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).


import pytest

from bootstrap.dependency_container.container_bootstrap import build_container, initialize_container


def test_initialize_container_smoke():
    """Smoke test for module-level function initialize_container."""
    try:
        initialize_container()
    except (Exception, SystemExit) as e:
        pytest.skip(f"initialize_container needs specific input data: {e}")
        return
    assert True


def test_build_container_smoke():
    """Smoke test for module-level function build_container."""
    try:
        build_container()
    except (Exception, SystemExit) as e:
        pytest.skip(f"build_container needs specific input data: {e}")
        return
    assert True
