# AUTO-GENERATED TESTS for adapters/primary_api/v1/fastapi_bank_cash_router.py
# =========================================
# Regenerated by gen_tests.py (AST-based, real code)
# Source module: adapters.primary_api.v1.fastapi_bank_cash_router
# Note: constructor/method smoke tests use mocked collaborators;
#       tests pytest.skip() with the real exception message when a
#       method genuinely needs live infra (DB/broker/etc).

from datetime import UTC, date, datetime
from decimal import Decimal
from unittest.mock import MagicMock
from uuid import uuid4

import pytest

from adapters.primary_api.v1.fastapi_bank_cash_router import (
    AccountBalanceHistorySchema,
    BankAccountCreateSchema,
    BankAccountResponseSchema,
    BankAccountStatus,
    BankAccountType,
    BankAccountUpdateSchema,
    BankReconciliationCreateSchema,
    BankReconciliationResponseSchema,
    BankTransactionCreateSchema,
    BankTransactionResponseSchema,
    BankTransactionReverseSchema,
    BankTransactionUpdateSchema,
    BankTransferApproveSchema,
    BankTransferCreateSchema,
    BankTransferResponseSchema,
    CashBookCreateSchema,
    CashBookRepositoryAdapter,
    CashBookResponseSchema,
    CashBookStatus,
    CashBookUpdateSchema,
    CashFlowReportSchema,
    CashFlowRepositoryAdapter,
    CashTransactionCreateSchema,
    CashTransactionResponseSchema,
    DailyCashPositionSchema,
    IdempotencyManager,
    PettyCashCreateSchema,
    PettyCashReimbursementSchema,
    PettyCashResponseSchema,
    PettyCashStatus,
    ReconciliationStatus,
    TransactionStatus,
    TransferStatus,
    activate_bank_account,
    approve_bank_transfer,
    cancel_bank_transfer,
    close_reconciliation,
    create_bank_account,
    create_bank_transaction,
    create_bank_transfer,
    create_cash_book,
    create_petty_cash_fund,
    deactivate_bank_account,
    export_bank_transactions,
    get_bank_account,
    get_bank_balance,
    get_bank_balance_history,
    get_bank_cash_service,
    get_bank_reconciliation_use_case,
    get_bank_transaction,
    get_cash_book_balance,
    get_cash_book_by_id,
    get_cash_book_transactions,
    get_cash_books_by_currency,
    get_cash_flow_report,
    get_daily_cash_position,
    get_reconciliation_history,
    health,
    import_bank_statement,
    info,
    list_bank_accounts,
    list_bank_transactions,
    list_cash_books,
    lock_bank_account,
    ping,
    process_bank_transfer,
    reconcile_bank,
    record_cash_transaction,
    reimburse_petty_cash,
    reverse_bank_transaction,
    unlock_bank_account,
    update_bank_account,
    update_bank_transaction,
    update_cash_book,
)


class TestIdempotencyManager:
    """Tests for IdempotencyManager."""

    def _build_instance(self):
        return IdempotencyManager()

    def test_construction(self):
        """IdempotencyManager can be instantiated with mocked dependencies."""
        try:
            instance = self._build_instance()
        except (Exception, SystemExit) as e:
            pytest.skip(f"Requires domain-specific construction setup: {e}")
            return
        assert isinstance(instance, IdempotencyManager)

    def test_get_cached_result_returns_none_for_missing_key(self):
        instance = IdempotencyManager()
        result = instance.get_cached_result("non_existent_key", "method")
        assert result is None

    def test_cache_result_returns_true_on_success(self):
        instance = IdempotencyManager()
        result = instance.cache_result("key", "method", {"data": "value"})
        assert result is True


class TestBankAccountType:
    """Tests for the BankAccountType enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(BankAccountType, 'CHECKING')
        assert hasattr(BankAccountType, 'SAVINGS')
        assert hasattr(BankAccountType, 'DEPOSIT')
        assert hasattr(BankAccountType, 'LOAN')
        assert hasattr(BankAccountType, 'CREDIT_CARD')
        assert hasattr(BankAccountType, 'PETTY_CASH')
        assert hasattr(BankAccountType, 'CASH_ON_HAND')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(BankAccountType.CHECKING, BankAccountType)


class TestBankAccountStatus:
    """Tests for the BankAccountStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(BankAccountStatus, 'ACTIVE')
        assert hasattr(BankAccountStatus, 'INACTIVE')
        assert hasattr(BankAccountStatus, 'SUSPENDED')
        assert hasattr(BankAccountStatus, 'CLOSED')
        assert hasattr(BankAccountStatus, 'LOCKED')
        assert hasattr(BankAccountStatus, 'ARCHIVED')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(BankAccountStatus.ACTIVE, BankAccountStatus)


class TestTransactionStatus:
    """Tests for the TransactionStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(TransactionStatus, 'DRAFT')
        assert hasattr(TransactionStatus, 'PENDING')
        assert hasattr(TransactionStatus, 'POSTED')
        assert hasattr(TransactionStatus, 'CLEARED')
        assert hasattr(TransactionStatus, 'REVERSED')
        assert hasattr(TransactionStatus, 'CANCELLED')
        assert hasattr(TransactionStatus, 'VOID')
        assert hasattr(TransactionStatus, 'RECONCILED')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(TransactionStatus.DRAFT, TransactionStatus)


class TestReconciliationStatus:
    """Tests for the ReconciliationStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(ReconciliationStatus, 'DRAFT')
        assert hasattr(ReconciliationStatus, 'IN_PROGRESS')
        assert hasattr(ReconciliationStatus, 'COMPLETED')
        assert hasattr(ReconciliationStatus, 'CLOSED')
        assert hasattr(ReconciliationStatus, 'CANCELLED')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(ReconciliationStatus.DRAFT, ReconciliationStatus)


class TestCashBookStatus:
    """Tests for the CashBookStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(CashBookStatus, 'ACTIVE')
        assert hasattr(CashBookStatus, 'CLOSED')
        assert hasattr(CashBookStatus, 'LOCKED')
        assert hasattr(CashBookStatus, 'ARCHIVED')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(CashBookStatus.ACTIVE, CashBookStatus)


class TestPettyCashStatus:
    """Tests for the PettyCashStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(PettyCashStatus, 'ACTIVE')
        assert hasattr(PettyCashStatus, 'REIMBURSED')
        assert hasattr(PettyCashStatus, 'CLOSED')
        assert hasattr(PettyCashStatus, 'LOCKED')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(PettyCashStatus.ACTIVE, PettyCashStatus)


class TestTransferStatus:
    """Tests for the TransferStatus enum."""
    def test_members_exist(self):
        """All expected enum members are defined."""
        assert hasattr(TransferStatus, 'DRAFT')
        assert hasattr(TransferStatus, 'SUBMITTED')
        assert hasattr(TransferStatus, 'APPROVED')
        assert hasattr(TransferStatus, 'PROCESSED')
        assert hasattr(TransferStatus, 'COMPLETED')
        assert hasattr(TransferStatus, 'REJECTED')
        assert hasattr(TransferStatus, 'CANCELLED')
        assert hasattr(TransferStatus, 'REVERSED')

    def test_member_is_instance(self):
        """Enum members are instances of the enum class."""
        assert isinstance(TransferStatus.DRAFT, TransferStatus)


class TestBankAccountCreateSchema:
    """Tests for the BankAccountCreateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            account_number="test_value",
            account_name="test_value",
            bank_name="test_value",
            bank_code="test_value",
            currency_code="test_value",
            account_type=BankAccountType.CHECKING,
            opening_balance=Decimal("100.00"),
            opening_balance_date=date.today(),
            gl_account_id=uuid4(),
            is_active=True,
            is_default=True,
            bank_address="test_value",
            swift_code="test_value",
            iban="test_value",
            notes="test_value",
            daily_limit=Decimal("100.00"),
            transaction_limit=Decimal("100.00"),
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankAccountCreateSchema(**kwargs)
        assert isinstance(instance, BankAccountCreateSchema)
        assert instance.account_number == kwargs['account_number']


class TestBankAccountUpdateSchema:
    """Tests for the BankAccountUpdateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            account_name="test_value",
            is_active=True,
            is_default=True,
            bank_address="test_value",
            notes="test_value",
            daily_limit=Decimal("100.00"),
            transaction_limit=Decimal("100.00"),
            status=BankAccountStatus.ACTIVE,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankAccountUpdateSchema(**kwargs)
        assert isinstance(instance, BankAccountUpdateSchema)
        assert instance.account_name == kwargs['account_name']


class TestBankAccountResponseSchema:
    """Tests for the BankAccountResponseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            account_number="test_value",
            account_name="test_value",
            bank_name="test_value",
            bank_code="test_value",
            currency_code="test_value",
            account_type=BankAccountType.CHECKING,
            current_balance=Decimal("100.00"),
            available_balance=Decimal("100.00"),
            opening_balance=Decimal("100.00"),
            opening_balance_date=date.today(),
            gl_account_id=uuid4(),
            status=BankAccountStatus.ACTIVE,
            is_active=True,
            is_default=True,
            is_locked=True,
            bank_address="test_value",
            swift_code="test_value",
            iban="test_value",
            daily_limit=Decimal("100.00"),
            transaction_limit=Decimal("100.00"),
            notes="test_value",
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            created_by_name="test_value",
            updated_at=datetime.now(UTC),
            updated_by=uuid4(),
            version=1,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankAccountResponseSchema(**kwargs)
        assert isinstance(instance, BankAccountResponseSchema)
        assert instance.id == kwargs['id']


class TestBankTransactionCreateSchema:
    """Tests for the BankTransactionCreateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            bank_account_id=uuid4(),
            transaction_date=date.today(),
            transaction_type=MagicMock(),
            amount=Decimal("100.00"),
            description="test_value",
            reference_number="test_value",
            counterparty_account="test_value",
            counterparty_name="test_value",
            transfer_to_account_id=uuid4(),
            post_to_ledger=True,
            notes="test_value",
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankTransactionCreateSchema(**kwargs)
        assert isinstance(instance, BankTransactionCreateSchema)
        assert instance.bank_account_id == kwargs['bank_account_id']


class TestBankTransactionUpdateSchema:
    """Tests for the BankTransactionUpdateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            description="test_value",
            reference_number="test_value",
            notes="test_value",
            status=TransactionStatus.DRAFT,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankTransactionUpdateSchema(**kwargs)
        assert isinstance(instance, BankTransactionUpdateSchema)
        assert instance.description == kwargs['description']


class TestBankTransactionResponseSchema:
    """Tests for the BankTransactionResponseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            transaction_number="test_value",
            bank_account_id=uuid4(),
            bank_account_name="test_value",
            transaction_date=date.today(),
            transaction_type=MagicMock(),
            amount=Decimal("100.00"),
            description="test_value",
            reference_number="test_value",
            counterparty_account="test_value",
            counterparty_name="test_value",
            journal_id=uuid4(),
            status=TransactionStatus.DRAFT,
            reconciled_at=datetime.now(UTC),
            reconciliation_id=uuid4(),
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            created_by_name="test_value",
            version=1,
            is_reversed=True,
            reversed_at=datetime.now(UTC),
            reversed_by=uuid4(),
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankTransactionResponseSchema(**kwargs)
        assert isinstance(instance, BankTransactionResponseSchema)
        assert instance.id == kwargs['id']


class TestBankTransactionReverseSchema:
    """Tests for the BankTransactionReverseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            reason="test_value",
            reversal_date=date.today(),
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankTransactionReverseSchema(**kwargs)
        assert isinstance(instance, BankTransactionReverseSchema)
        assert instance.reason == kwargs['reason']


class TestBankReconciliationCreateSchema:
    """Tests for the BankReconciliationCreateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            bank_account_id=uuid4(),
            statement_date=date.today(),
            statement_balance=Decimal("100.00"),
            statement_transactions=[{}],
            auto_match_threshold=Decimal("100.00"),
            notes="test_value",
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankReconciliationCreateSchema(**kwargs)
        assert isinstance(instance, BankReconciliationCreateSchema)
        assert instance.bank_account_id == kwargs['bank_account_id']


class TestBankReconciliationResponseSchema:
    """Tests for the BankReconciliationResponseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            reconciliation_number="test_value",
            bank_account_id=uuid4(),
            bank_account_name="test_value",
            statement_date=date.today(),
            statement_balance=Decimal("100.00"),
            book_balance=Decimal("100.00"),
            difference=Decimal("100.00"),
            matched_count=1,
            unmatched_book_count=1,
            unmatched_statement_count=1,
            adjustment_amount=Decimal("100.00"),
            adjustment_journal_id=uuid4(),
            status=ReconciliationStatus.DRAFT,
            notes="test_value",
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            created_by_name="test_value",
            completed_at=datetime.now(UTC),
            completed_by=uuid4(),
            version=1,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankReconciliationResponseSchema(**kwargs)
        assert isinstance(instance, BankReconciliationResponseSchema)
        assert instance.id == kwargs['id']


class TestCashBookCreateSchema:
    """Tests for the CashBookCreateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            name="test_value",
            currency_code="test_value",
            opening_balance=Decimal("100.00"),
            opening_balance_date=date.today(),
            gl_cash_account_id=uuid4(),
            gl_bank_account_id=uuid4(),
            location="test_value",
            custodian_id=uuid4(),
            min_balance=Decimal("100.00"),
            max_balance=Decimal("100.00"),
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = CashBookCreateSchema(**kwargs)
        assert isinstance(instance, CashBookCreateSchema)
        assert instance.name == kwargs['name']


class TestCashBookResponseSchema:
    """Tests for the CashBookResponseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            name="test_value",
            currency_code="test_value",
            current_balance=Decimal("100.00"),
            opening_balance=Decimal("100.00"),
            opening_balance_date=date.today(),
            gl_cash_account_id=uuid4(),
            gl_bank_account_id=uuid4(),
            status=CashBookStatus.ACTIVE,
            location="test_value",
            custodian_id=uuid4(),
            custodian_name="test_value",
            min_balance=Decimal("100.00"),
            max_balance=Decimal("100.00"),
            is_locked=True,
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            created_by_name="test_value",
            updated_at=datetime.now(UTC),
            version=1,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = CashBookResponseSchema(**kwargs)
        assert isinstance(instance, CashBookResponseSchema)
        assert instance.id == kwargs['id']


class TestCashBookUpdateSchema:
    """Tests for the CashBookUpdateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            name="test_value",
            status=CashBookStatus.ACTIVE,
            min_balance=Decimal("100.00"),
            max_balance=Decimal("100.00"),
            location="test_value",
            custodian_id=uuid4(),
            is_locked=True,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = CashBookUpdateSchema(**kwargs)
        assert isinstance(instance, CashBookUpdateSchema)
        assert instance.name == kwargs['name']


class TestCashTransactionCreateSchema:
    """Tests for the CashTransactionCreateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            cash_book_id=uuid4(),
            transaction_date=date.today(),
            transaction_type=MagicMock(),
            amount=Decimal("100.00"),
            description="test_value",
            reference_number="test_value",
            counterparty_name="test_value",
            post_to_ledger=True,
            notes="test_value",
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = CashTransactionCreateSchema(**kwargs)
        assert isinstance(instance, CashTransactionCreateSchema)
        assert instance.cash_book_id == kwargs['cash_book_id']


class TestCashTransactionResponseSchema:
    """Tests for the CashTransactionResponseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            transaction_number="test_value",
            cash_book_id=uuid4(),
            cash_book_name="test_value",
            transaction_date=date.today(),
            transaction_type=MagicMock(),
            amount=Decimal("100.00"),
            description="test_value",
            reference_number="test_value",
            counterparty_name="test_value",
            journal_id=uuid4(),
            status=TransactionStatus.DRAFT,
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            created_by_name="test_value",
            version=1,
            is_reversed=True,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = CashTransactionResponseSchema(**kwargs)
        assert isinstance(instance, CashTransactionResponseSchema)
        assert instance.id == kwargs['id']


class TestPettyCashCreateSchema:
    """Tests for the PettyCashCreateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            fund_name="test_value",
            currency_code="test_value",
            initial_amount=Decimal("100.00"),
            custodian_id=uuid4(),
            gl_petty_cash_account_id=uuid4(),
            reimbursement_threshold=Decimal("100.00"),
            fund_location="test_value",
            notes="test_value",
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = PettyCashCreateSchema(**kwargs)
        assert isinstance(instance, PettyCashCreateSchema)
        assert instance.fund_name == kwargs['fund_name']


class TestPettyCashResponseSchema:
    """Tests for the PettyCashResponseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            fund_name="test_value",
            currency_code="test_value",
            current_balance=Decimal("100.00"),
            initial_amount=Decimal("100.00"),
            custodian_id=uuid4(),
            custodian_name="test_value",
            gl_account_id=uuid4(),
            reimbursement_threshold=Decimal("100.00"),
            status=PettyCashStatus.ACTIVE,
            fund_location="test_value",
            notes="test_value",
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            created_by_name="test_value",
            version=1,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = PettyCashResponseSchema(**kwargs)
        assert isinstance(instance, PettyCashResponseSchema)
        assert instance.id == kwargs['id']


class TestPettyCashReimbursementSchema:
    """Tests for the PettyCashReimbursementSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            reimbursement_date=date.today(),
            amount=Decimal("100.00"),
            bank_account_id=uuid4(),
            description="test_value",
            notes="test_value",
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = PettyCashReimbursementSchema(**kwargs)
        assert isinstance(instance, PettyCashReimbursementSchema)
        assert instance.reimbursement_date == kwargs['reimbursement_date']


class TestBankTransferCreateSchema:
    """Tests for the BankTransferCreateSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            from_bank_account_id=uuid4(),
            to_bank_account_id=uuid4(),
            transfer_date=date.today(),
            amount=Decimal("100.00"),
            description="test_value",
            reference_number="test_value",
            notes="test_value",
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankTransferCreateSchema(**kwargs)
        assert isinstance(instance, BankTransferCreateSchema)
        assert instance.from_bank_account_id == kwargs['from_bank_account_id']


class TestBankTransferResponseSchema:
    """Tests for the BankTransferResponseSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            id=uuid4(),
            transfer_number="test_value",
            from_account_id=uuid4(),
            from_account_name="test_value",
            to_account_id=uuid4(),
            to_account_name="test_value",
            transfer_date=date.today(),
            amount=Decimal("100.00"),
            description="test_value",
            reference_number="test_value",
            notes="test_value",
            status=TransferStatus.DRAFT,
            from_journal_id=uuid4(),
            to_journal_id=uuid4(),
            created_at=datetime.now(UTC),
            created_by=uuid4(),
            created_by_name="test_value",
            approved_at=datetime.now(UTC),
            approved_by=uuid4(),
            processed_at=datetime.now(UTC),
            version=1,
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankTransferResponseSchema(**kwargs)
        assert isinstance(instance, BankTransferResponseSchema)
        assert instance.id == kwargs['id']


class TestBankTransferApproveSchema:
    """Tests for the BankTransferApproveSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            approved=True,
            notes="test_value",
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = BankTransferApproveSchema(**kwargs)
        assert isinstance(instance, BankTransferApproveSchema)
        assert instance.approved == kwargs['approved']


class TestCashFlowReportSchema:
    """Tests for the CashFlowReportSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            legal_entity_id=uuid4(),
            start_date=date.today(),
            end_date=date.today(),
            beginning_cash=Decimal("100.00"),
            cash_receipts=Decimal("100.00"),
            cash_disbursements=Decimal("100.00"),
            net_cash_flow=Decimal("100.00"),
            ending_cash=Decimal("100.00"),
            by_category={},
            generated_at=datetime.now(UTC),
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = CashFlowReportSchema(**kwargs)
        assert isinstance(instance, CashFlowReportSchema)
        assert instance.legal_entity_id == kwargs['legal_entity_id']


class TestDailyCashPositionSchema:
    """Tests for the DailyCashPositionSchema value object / model."""

    def _build_kwargs(self):
        return dict(
            as_of_date=date.today(),
            account_type="test_value",
            account_id=uuid4(),
            account_name="test_value",
            currency="test_value",
            balance=Decimal("100.00"),
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = DailyCashPositionSchema(**kwargs)
        assert isinstance(instance, DailyCashPositionSchema)
        assert instance.as_of_date == kwargs['as_of_date']


class TestAccountBalanceHistorySchema:
    """Tests for the AccountBalanceHistorySchema value object / model."""

    def _build_kwargs(self):
        return dict(
            as_of_date=date.today(),
            balance=Decimal("100.00"),
            available_balance=Decimal("100.00"),
            change_from_previous=Decimal("100.00"),
        )

    def test_construction_success(self):
        kwargs = self._build_kwargs()
        instance = AccountBalanceHistorySchema(**kwargs)
        assert isinstance(instance, AccountBalanceHistorySchema)
        assert instance.as_of_date == kwargs['as_of_date']


class TestCashBookRepositoryAdapter:
    """Tests for CashBookRepositoryAdapter."""

    def _build_instance(self):
        return CashBookRepositoryAdapter()

    def test_construction(self):
        instance = self._build_instance()
        assert isinstance(instance, CashBookRepositoryAdapter)

    async def test_add_returns_not_implemented(self):
        instance = self._build_instance()
        with pytest.raises(NotImplementedError):
            await instance.add(cash_book=MagicMock())

    async def test_get_balance_returns_not_implemented(self):
        instance = self._build_instance()
        with pytest.raises(NotImplementedError):
            await instance.get_balance(cash_book_id=uuid4(), as_of_date=date.today())

    async def test_get_by_id_returns_not_implemented(self):
        instance = self._build_instance()
        with pytest.raises(NotImplementedError):
            await instance.get_by_id(cash_book_id=uuid4())

    async def test_get_by_legal_entity_and_currency_returns_not_implemented(self):
        instance = self._build_instance()
        with pytest.raises(NotImplementedError):
            await instance.get_by_legal_entity_and_currency(legal_entity_id=uuid4(), currency_code="test_value")


class TestCashFlowRepositoryAdapter:
    """Tests for CashFlowRepositoryAdapter."""

    def _build_instance(self):
        return CashFlowRepositoryAdapter()

    def test_construction(self):
        instance = self._build_instance()
        assert isinstance(instance, CashFlowRepositoryAdapter)

    async def test_get_cash_flow_returns_not_implemented(self):
        instance = self._build_instance()
        with pytest.raises(NotImplementedError):
            await instance.get_cash_flow(legal_entity_id=uuid4(), start_date=date.today(), end_date=date.today(), account_type="test_value")


# ============================================================================
# MODULE-LEVEL FUNCTIONS (ROUTER ENDPOINTS)
# ============================================================================

async def test_get_bank_cash_service():
    """Test get_bank_cash_service returns a service instance."""
    request = MagicMock()
    result = await get_bank_cash_service(request=request)
    assert result is not None


async def test_get_bank_reconciliation_use_case():
    """Test get_bank_reconciliation_use_case returns a use case instance."""
    request = MagicMock()
    result = await get_bank_reconciliation_use_case(request=request)
    assert result is not None


def test_ping_returns_pong():
    """Test ping endpoint returns expected response."""
    result = ping()
    assert isinstance(result, dict)
    assert result.get("status") == "ok" or "pong" in str(result)


def test_health_returns_status():
    """Test health endpoint returns health status."""
    result = health()
    assert isinstance(result, dict)
    assert "status" in result


def test_info_returns_info():
    """Test info endpoint returns service information."""
    result = info()
    assert isinstance(result, dict)
    assert "version" in result or "name" in result


async def test_create_bank_account_returns_response():
    request = MagicMock()
    service = MagicMock()
    service.create_bank_account = MagicMock(return_value={"id": str(uuid4())})
    result = await create_bank_account(
        request=request,
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert isinstance(result, dict)
    assert "id" in result


async def test_list_bank_accounts_returns_list():
    service = MagicMock()
    service.list_bank_accounts = MagicMock(return_value=[])
    result = await list_bank_accounts(
        account_type=BankAccountType.CHECKING,
        currency="IDR",
        is_active=True,
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_get_bank_account_returns_account():
    service = MagicMock()
    service.get_bank_account = MagicMock(return_value={"id": str(uuid4())})
    result = await get_bank_account(
        account_id=uuid4(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_update_bank_account_returns_updated():
    service = MagicMock()
    service.update_bank_account = MagicMock(return_value={"id": str(uuid4())})
    result = await update_bank_account(
        account_id=uuid4(),
        request=MagicMock(),
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_deactivate_bank_account_returns_success():
    service = MagicMock()
    service.deactivate_bank_account = MagicMock(return_value={"success": True})
    result = await deactivate_bank_account(
        account_id=uuid4(),
        permanent=True,
        reason="test",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_activate_bank_account_returns_success():
    service = MagicMock()
    service.activate_bank_account = MagicMock(return_value={"success": True})
    result = await activate_bank_account(
        account_id=uuid4(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_lock_bank_account_returns_success():
    service = MagicMock()
    service.lock_bank_account = MagicMock(return_value={"success": True})
    result = await lock_bank_account(
        account_id=uuid4(),
        reason="test",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_unlock_bank_account_returns_success():
    service = MagicMock()
    service.unlock_bank_account = MagicMock(return_value={"success": True})
    result = await unlock_bank_account(
        account_id=uuid4(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_create_bank_transaction_returns_transaction():
    service = MagicMock()
    service.create_bank_transaction = MagicMock(return_value={"id": str(uuid4())})
    result = await create_bank_transaction(
        request=MagicMock(),
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_get_bank_transaction_returns_transaction():
    service = MagicMock()
    service.get_bank_transaction = MagicMock(return_value={"id": str(uuid4())})
    result = await get_bank_transaction(
        transaction_id=uuid4(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_update_bank_transaction_returns_updated():
    service = MagicMock()
    service.update_bank_transaction = MagicMock(return_value={"id": str(uuid4())})
    result = await update_bank_transaction(
        transaction_id=uuid4(),
        request=MagicMock(),
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_reverse_bank_transaction_returns_reversed():
    service = MagicMock()
    service.reverse_bank_transaction = MagicMock(return_value={"id": str(uuid4())})
    result = await reverse_bank_transaction(
        transaction_id=uuid4(),
        request=MagicMock(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_list_bank_transactions_returns_list():
    service = MagicMock()
    service.list_bank_transactions = MagicMock(return_value=[])
    result = await list_bank_transactions(
        bank_account_id=uuid4(),
        start_date=date.today(),
        end_date=date.today(),
        transaction_type=MagicMock(),
        status=TransactionStatus.DRAFT,
        page=1,
        page_size=10,
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_import_bank_statement_returns_success():
    service = MagicMock()
    service.import_bank_statement = MagicMock(return_value={"success": True})
    result = await import_bank_statement(
        file=MagicMock(),
        bank_account_id=uuid4(),
        statement_date=date.today(),
        file_format="csv",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_reconcile_bank_returns_reconciliation():
    use_case = MagicMock()
    use_case.execute = MagicMock(return_value={"id": str(uuid4())})
    result = await reconcile_bank(
        request=MagicMock(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        use_case=use_case
    )
    assert result is not None
    assert "id" in result


async def test_get_reconciliation_history_returns_list():
    service = MagicMock()
    service.get_reconciliation_history = MagicMock(return_value=[])
    result = await get_reconciliation_history(
        bank_account_id=uuid4(),
        limit=10,
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_close_reconciliation_returns_success():
    service = MagicMock()
    service.close_reconciliation = MagicMock(return_value={"success": True})
    result = await close_reconciliation(
        reconciliation_id=uuid4(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_create_cash_book_returns_cash_book():
    service = MagicMock()
    service.create_cash_book = MagicMock(return_value={"id": str(uuid4())})
    result = await create_cash_book(
        request=MagicMock(),
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_list_cash_books_returns_list():
    service = MagicMock()
    service.list_cash_books = MagicMock(return_value=[])
    result = await list_cash_books(
        status=CashBookStatus.ACTIVE,
        custodian_id=uuid4(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_get_cash_book_by_id_returns_cash_book():
    service = MagicMock()
    service.get_cash_book_by_id = MagicMock(return_value={"id": str(uuid4())})
    result = await get_cash_book_by_id(
        cash_book_id=uuid4(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_get_cash_books_by_currency_returns_list():
    service = MagicMock()
    service.get_cash_books_by_currency = MagicMock(return_value=[])
    result = await get_cash_books_by_currency(
        currency_code="IDR",
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_get_cash_book_transactions_returns_list():
    service = MagicMock()
    service.get_cash_book_transactions = MagicMock(return_value=[])
    result = await get_cash_book_transactions(
        cash_book_id=uuid4(),
        start_date=date.today(),
        end_date=date.today(),
        limit=10,
        offset=0,
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_update_cash_book_returns_updated():
    service = MagicMock()
    service.update_cash_book = MagicMock(return_value={"id": str(uuid4())})
    result = await update_cash_book(
        cash_book_id=uuid4(),
        request=MagicMock(),
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_get_cash_book_balance_returns_balance():
    service = MagicMock()
    service.get_cash_book_balance = MagicMock(return_value={"balance": 100.0})
    result = await get_cash_book_balance(
        cash_book_id=uuid4(),
        as_of_date=date.today(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "balance" in result


async def test_record_cash_transaction_returns_transaction():
    service = MagicMock()
    service.record_cash_transaction = MagicMock(return_value={"id": str(uuid4())})
    result = await record_cash_transaction(
        request=MagicMock(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_create_petty_cash_fund_returns_fund():
    service = MagicMock()
    service.create_petty_cash_fund = MagicMock(return_value={"id": str(uuid4())})
    result = await create_petty_cash_fund(
        request=MagicMock(),
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_reimburse_petty_cash_returns_success():
    service = MagicMock()
    service.reimburse_petty_cash = MagicMock(return_value={"success": True})
    result = await reimburse_petty_cash(
        fund_id=uuid4(),
        request=MagicMock(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_create_bank_transfer_returns_transfer():
    service = MagicMock()
    service.create_bank_transfer = MagicMock(return_value={"id": str(uuid4())})
    result = await create_bank_transfer(
        request=MagicMock(),
        idempotency_key="key",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_approve_bank_transfer_returns_approved():
    service = MagicMock()
    service.approve_bank_transfer = MagicMock(return_value={"id": str(uuid4())})
    result = await approve_bank_transfer(
        transfer_id=uuid4(),
        request=MagicMock(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_process_bank_transfer_returns_processed():
    service = MagicMock()
    service.process_bank_transfer = MagicMock(return_value={"id": str(uuid4())})
    result = await process_bank_transfer(
        transfer_id=uuid4(),
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "id" in result


async def test_cancel_bank_transfer_returns_success():
    service = MagicMock()
    service.cancel_bank_transfer = MagicMock(return_value={"success": True})
    result = await cancel_bank_transfer(
        transfer_id=uuid4(),
        reason="test",
        _permission=MagicMock(),
        current_user=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert result.get("success") is True


async def test_get_bank_balance_returns_balance():
    service = MagicMock()
    service.get_bank_balance = MagicMock(return_value={"balance": 100.0})
    result = await get_bank_balance(
        account_id=uuid4(),
        as_of_date=date.today(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "balance" in result


async def test_get_bank_balance_history_returns_list():
    service = MagicMock()
    service.get_bank_balance_history = MagicMock(return_value=[])
    result = await get_bank_balance_history(
        account_id=uuid4(),
        start_date=date.today(),
        end_date=date.today(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_get_cash_flow_report_returns_report():
    service = MagicMock()
    service.get_cash_flow_report = MagicMock(return_value={"cash_flow": []})
    result = await get_cash_flow_report(
        start_date=date.today(),
        end_date=date.today(),
        account_type=BankAccountType.CHECKING,
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "cash_flow" in result


async def test_get_daily_cash_position_returns_list():
    service = MagicMock()
    service.get_daily_cash_position = MagicMock(return_value=[])
    result = await get_daily_cash_position(
        as_of_date=date.today(),
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert isinstance(result, list)


async def test_export_bank_transactions_returns_export():
    service = MagicMock()
    service.export_bank_transactions = MagicMock(return_value={"file": "base64data"})
    result = await export_bank_transactions(
        bank_account_id=uuid4(),
        start_date=date.today(),
        end_date=date.today(),
        format="csv",
        _permission=MagicMock(),
        legal_entity_id=uuid4(),
        service=service
    )
    assert result is not None
    assert "file" in result
