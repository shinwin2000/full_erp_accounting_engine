#!/usr/bin/env python3
"""
Module: ledger_entry_partitioned_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'ledger_entry_partitioned'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

import sqlalchemy as sa
from sqlalchemy import BigInteger, Boolean, Date, DateTime, Float, Integer
from sqlalchemy import JSON, LargeBinary, Numeric, String, Text
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class LedgerEntryPartitionedTable(Base):
    """ORM model for table: ledger_entry_partitioned."""

    __tablename__ = "ledger_entry_partitioned"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    legal_entity_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    journal_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    journal_line_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    account_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    account_code: Mapped[str] = mapped_column(String(255), nullable=False)
    debit_amount: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    credit_amount: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    posting_date: Mapped[date] = mapped_column(Date(), nullable=False)
    fiscal_year: Mapped[int] = mapped_column(Integer(), nullable=False)
    period: Mapped[int] = mapped_column(Integer(), nullable=False)
    description: Mapped[str | None] = mapped_column(Text(), nullable=True)
    reference_number: Mapped[str | None] = mapped_column(String(255), nullable=True)
    created_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    created_by: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    hash_link: Mapped[str] = mapped_column(String(255), nullable=False)
    version: Mapped[int | None] = mapped_column(Integer(), nullable=True)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<LedgerEntryPartitionedTable id={pk}>"
