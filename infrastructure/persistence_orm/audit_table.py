#!/usr/bin/env python3
"""
Module: audit_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'audit'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

import uuid
from datetime import datetime
from uuid import UUID

from sqlalchemy import DateTime
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from infrastructure.persistence_orm.base_model import Base


class AuditTable(Base):
    """
    ORM model for table: audit.
    Model ini IMMUTABLE - tidak boleh di-update atau di-delete.
    """
    __tablename__ = "audit"
    # Flag untuk checker: model ini adalah audit log yang immutable
    __is_audit_log__ = True

    id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True),
        primary_key=True,
        default=uuid.uuid4,
        nullable=False,
    )
    # Tambahkan kolom yang mungkin diperlukan, misalnya created_at untuk timestamp
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=datetime.utcnow)
    # Jika ada kolom lain sesuai migrasi, tambahkan di sini.
    # Untuk sementara, cukup id dan created_at agar primary key terdefinisi.

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<AuditTable id={pk}>"
