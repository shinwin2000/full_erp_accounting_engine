#!/usr/bin/env python3
"""ORM for table: payroll_payslip. Auto-generated by P42 final sync."""
from __future__ import annotations

from datetime import datetime
from decimal import Decimal
from uuid import UUID

from sqlalchemy import DateTime, Integer, Numeric, String
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class PayrollPayslipOrmTable(Base):
    __tablename__ = "payroll_payslip"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    legal_entity_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    employee_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    payroll_run_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    period_year: Mapped[int] = mapped_column(Integer(), nullable=False)
    period_month: Mapped[int] = mapped_column(Integer(), nullable=False)
    gross_pay: Mapped[Decimal] = mapped_column(Numeric(20,6), nullable=False)
    deductions: Mapped[Decimal] = mapped_column(Numeric(20,6), nullable=False)
    net_pay: Mapped[Decimal] = mapped_column(Numeric(20,6), nullable=False)
    pdf_url: Mapped[str | None] = mapped_column(String(255), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)

    def __repr__(self) -> str:
        return f"<PayrollPayslipOrmTable id={getattr(self, 'id', '?')}>"
