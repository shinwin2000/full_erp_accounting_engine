#!/usr/bin/env python3
"""
Module: derivative_instrument_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'derivative_instrument'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

from sqlalchemy import (
    Boolean,
    Date,
    DateTime,
    Integer,
    Numeric,
    String,
)
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class DerivativeInstrumentTable(Base):
    """ORM model for table: derivative_instrument."""

    __tablename__ = "derivative_instrument"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    legal_entity_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    instrument_code: Mapped[str] = mapped_column(String(255), nullable=False)
    instrument_type: Mapped[str] = mapped_column(String(255), nullable=False)
    counterparty_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    underlying_asset: Mapped[str] = mapped_column(String(255), nullable=False)
    notional_amount: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    currency_code: Mapped[str] = mapped_column(String(255), nullable=False)
    settlement_date: Mapped[date | None] = mapped_column(Date(), nullable=True)
    maturity_date: Mapped[date] = mapped_column(Date(), nullable=False)
    strike_price: Mapped[Decimal | None] = mapped_column(Numeric(20, 6), nullable=True)
    premium_paid: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    fair_value_at_reporting: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    valuation_method: Mapped[str] = mapped_column(String(255), nullable=False)
    is_designated_hedge: Mapped[bool] = mapped_column(Boolean(), nullable=False)
    hedging_relationship_id: Mapped[UUID | None] = mapped_column(PG_UUID(as_uuid=True), nullable=True)
    status: Mapped[str] = mapped_column(String(255), nullable=False)
    version: Mapped[int] = mapped_column(Integer(), nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    created_by: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<DerivativeInstrumentTable id={pk}>"
