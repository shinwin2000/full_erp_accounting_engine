#!/usr/bin/env python3
"""
Module: coretax_webhook_inbound_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'coretax_webhook_inbound'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

import sqlalchemy as sa
from sqlalchemy import BigInteger, Boolean, Date, DateTime, Float, Integer
from sqlalchemy import JSON, LargeBinary, Numeric, String, Text
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class CoretaxWebhookInboundTable(Base):
    """ORM model for table: coretax_webhook_inbound."""

    __tablename__ = "coretax_webhook_inbound"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    legal_entity_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    webhook_type: Mapped[str] = mapped_column(String(255), nullable=False)
    payload: Mapped[str] = mapped_column(JSONB(), nullable=False)
    received_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    processed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    status: Mapped[str] = mapped_column(String(255), nullable=False)
    error_message: Mapped[str | None] = mapped_column(Text(), nullable=True)
    signature_verified: Mapped[bool] = mapped_column(Boolean(), nullable=False)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<CoretaxWebhookInboundTable id={pk}>"
