#!/usr/bin/env python3
"""
Module: integrity_check_result_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'integrity_check_result'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

import sqlalchemy as sa
from sqlalchemy import BigInteger, Boolean, Date, DateTime, Float, Integer
from sqlalchemy import JSON, LargeBinary, Numeric, String, Text
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class IntegrityCheckResultTable(Base):
    """ORM model for table: integrity_check_result."""

    __tablename__ = "integrity_check_result"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    check_started_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    check_completed_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    chain_type: Mapped[str] = mapped_column(String(255), nullable=False)
    chain_id_start: Mapped[UUID | None] = mapped_column(PG_UUID(as_uuid=True), nullable=True)
    chain_id_end: Mapped[UUID | None] = mapped_column(PG_UUID(as_uuid=True), nullable=True)
    total_entries_checked: Mapped[int] = mapped_column(BigInteger(), nullable=False)
    total_entries_valid: Mapped[int] = mapped_column(BigInteger(), nullable=False)
    total_entries_invalid: Mapped[int] = mapped_column(BigInteger(), nullable=False)
    invalid_chain_ids: Mapped[str | None] = mapped_column(JSONB(), nullable=True)
    status: Mapped[str] = mapped_column(String(255), nullable=False)
    report_path: Mapped[str | None] = mapped_column(String(255), nullable=True)
    triggered_by: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<IntegrityCheckResultTable id={pk}>"
