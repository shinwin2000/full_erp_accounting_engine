#!/usr/bin/env python3
"""
Module: outbox_kafka_partition_checkpoint_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'outbox_kafka_partition_checkpoint'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

import sqlalchemy as sa
from sqlalchemy import BigInteger, Boolean, Date, DateTime, Float, Integer
from sqlalchemy import JSON, LargeBinary, Numeric, String, Text
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class OutboxKafkaPartitionCheckpointTable(Base):
    """ORM model for table: outbox_kafka_partition_checkpoint."""

    __tablename__ = "outbox_kafka_partition_checkpoint"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    topic: Mapped[str] = mapped_column(String(255), nullable=False)
    partition: Mapped[int] = mapped_column(Integer(), nullable=False)
    consumer_group: Mapped[str] = mapped_column(String(255), nullable=False)
    last_offset: Mapped[int] = mapped_column(BigInteger(), nullable=False)
    last_committed_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    committed_by: Mapped[str] = mapped_column(String(255), nullable=False)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<OutboxKafkaPartitionCheckpointTable id={pk}>"
