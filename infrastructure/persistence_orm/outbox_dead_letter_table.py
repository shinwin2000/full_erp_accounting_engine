#!/usr/bin/env python3
"""
Module: outbox_dead_letter_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'outbox_dead_letter'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import datetime
from uuid import UUID

from sqlalchemy import (
    DateTime,
    String,
    Text,
)
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class OutboxDeadLetterTable(Base):
    """ORM model for table: outbox_dead_letter."""

    __tablename__ = "outbox_dead_letter"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    original_outbox_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    aggregate_type: Mapped[str] = mapped_column(String(255), nullable=False)
    aggregate_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    event_type: Mapped[str] = mapped_column(String(255), nullable=False)
    payload: Mapped[str] = mapped_column(JSONB(), nullable=False)
    extra_metadata: Mapped[str | None] = mapped_column(
        "metadata", JSONB(), nullable=True
    )  # 'metadata' is reserved in SQLAlchemy, mapped via column alias
    kafka_topic: Mapped[str] = mapped_column(String(255), nullable=False)
    final_error: Mapped[str] = mapped_column(Text(), nullable=False)
    failed_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    resolution_status: Mapped[str] = mapped_column(String(255), nullable=False)
    resolved_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    resolved_by: Mapped[UUID | None] = mapped_column(PG_UUID(as_uuid=True), nullable=True)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<OutboxDeadLetterTable id={pk}>"
