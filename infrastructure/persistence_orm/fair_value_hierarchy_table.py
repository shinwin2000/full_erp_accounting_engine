#!/usr/bin/env python3
"""
Module: fair_value_hierarchy_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'fair_value_hierarchy'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

import sqlalchemy as sa
from sqlalchemy import BigInteger, Boolean, Date, DateTime, Float, Integer
from sqlalchemy import JSON, LargeBinary, Numeric, String, Text
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class FairValueHierarchyTable(Base):
    """ORM model for table: fair_value_hierarchy."""

    __tablename__ = "fair_value_hierarchy"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    financial_instrument_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    instrument_type: Mapped[str] = mapped_column(String(255), nullable=False)
    valuation_date: Mapped[date] = mapped_column(Date(), nullable=False)
    fair_value: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    level_input: Mapped[int] = mapped_column(Integer(), nullable=False)
    level_description: Mapped[str | None] = mapped_column(Text(), nullable=True)
    unobservable_inputs: Mapped[str | None] = mapped_column(JSONB(), nullable=True)
    sensitivity_analysis: Mapped[str | None] = mapped_column(JSONB(), nullable=True)
    valuer_name: Mapped[str | None] = mapped_column(String(255), nullable=True)
    valuation_report_path: Mapped[str | None] = mapped_column(String(255), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<FairValueHierarchyTable id={pk}>"
