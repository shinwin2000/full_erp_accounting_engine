#!/usr/bin/env python3
"""ORM stub for table: bank_reconciliation. Auto-generated by P42 sync."""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

from sqlalchemy import Boolean, Date, DateTime, Numeric
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class BankReconciliationAliasTable(Base):
    __tablename__ = "bank_reconciliation"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    legal_entity_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    bank_account_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    reconciliation_date: Mapped[date] = mapped_column(Date(), nullable=False)
    statement_balance: Mapped[Decimal] = mapped_column(Numeric(20,6), nullable=False)
    book_balance: Mapped[Decimal] = mapped_column(Numeric(20,6), nullable=False)
    difference: Mapped[Decimal] = mapped_column(Numeric(20,6), nullable=False)
    is_reconciled: Mapped[bool] = mapped_column(Boolean(), nullable=False)
    reconciled_by: Mapped[UUID | None] = mapped_column(PG_UUID(as_uuid=True), nullable=True)
    reconciled_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    created_by: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    updated_by: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)

    def __repr__(self) -> str:
        return f"<BankReconciliationAliasTable id={getattr(self, 'id', '?')}>"
