#!/usr/bin/env python3
"""
Module: stock_opname_lines_table.py
Layer: Infrastructure (Persistence ORM)
Responsibility: ORM table definition for 'stock_opname_lines'.
Auto-generated by P42 sync fix.
"""
from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import UUID

import sqlalchemy as sa
from sqlalchemy import BigInteger, Boolean, Date, DateTime, Float, Integer
from sqlalchemy import JSON, LargeBinary, Numeric, String, Text
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import text

from infrastructure.persistence_orm.base_model import Base


class StockOpnameLinesTable(Base):
    """ORM model for table: stock_opname_lines."""

    __tablename__ = "stock_opname_lines"

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    opname_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    item_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False)
    system_quantity: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    physical_quantity: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    variance: Mapped[Decimal] = mapped_column(Numeric(20, 6), nullable=False)
    adjustment_journal_id: Mapped[UUID | None] = mapped_column(PG_UUID(as_uuid=True), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)

    def __repr__(self) -> str:
        pk = getattr(self, "id", "?")
        return f"<StockOpnameLinesTable id={pk}>"
